import docx

doc = docx.Document()

doc.add_heading('מסמך אפיון וסיכום מערכת: בעיות השוק והפתרונות', 0)

# 1. הקדמה
doc.add_heading('1. הקדמה ומטרת המוצר', level=1)
doc.add_paragraph('המערכת נועדה להוות AI Sales Copilot ו-CRM אוטונומי מבוסס WhatsApp, רשתות חברתיות ותמלול שיחות קוליות. המערכת הופכת את ערוצי התקשורת והשיחות הקוליות של העסק למכונת מכירות ששומרת, מנהלת, מזינה נתונים ומחזירה לחיים לידים באופן אוטומטי.')

# 2. הבעיות והפתרונות
doc.add_heading('2. מפת הבעיות והפתרונות המלאה', level=1)

p1 = doc.add_paragraph()
p1.add_run('א. Speed to Lead (מענה מיידי):\n').bold = True
p1.add_run('מענה אוטומטי אנושי בתוך חלון הזמנים הקריטי לשמירת הליד בשיחה.\n\n')

p1.add_run('ב. שמירת אנשי קשר ולידים "לא רלוונטיים":\n').bold = True
p1.add_run('קליטה אוטומטית של 100% מהפונים ושמירת לידים קפואים במאגר לטיפול עתידי.\n\n')

p1.add_run('ג. CRM נגיש ואוטונומי:\n').bold = True
p1.add_run('ניהול פייפליין מלא ללא הצורך בהקלדות ידניות או מערכות מסורבלות.\n\n')

p1.add_run('ד. מנוע החייאת לידים (Reactivation Engine):\n').bold = True
p1.add_run('סריקה מחזורית של המאגר ושליחת פניות מותאמות אישית ב-WhatsApp להחזרת לידים קרים לפייפליין.\n\n')

p1.add_run('ה. תמלול קולי, סיכום וחילוץ נתונים (Zero-Click Data Extraction & Call Summarization):\n').bold = True
p1.add_run('תמלול שיחות המכירה הקוליות, סיכום תוכן השיחה (מה נאמר ולמה העסקה נתקעה), חילוץ שדות (שם, שם משפחה, עיר, תקציב וכו\') והזנתם אוטומטית ל-CRM, לצד פנייה ב-WhatsApp להשלמת פרטים חסרים.')

# 3. ארכיטקטורה
doc.add_heading('3. ארכיטקטורת הפעולה', level=1)
doc.add_paragraph('1. קליטת הודעה (Webhook) או שיחה קולית (Audio Stream).\n2. מענה מיידי / תמלול השיחה, ניתוח תוכן השיחה וחילוץ שדות אוטומטי.\n3. יצירת/עדכון כרטיסיית לקוח ב-CRM וזיהוי שדות חסרים.\n4. פנייה אוטומטית ב-WhatsApp להשלמת פרטים במידת הצורך.\n5. הפעלת מנוע Reactivation תקופתי להחייאת דאטה קפואה.')

doc.save('System_Specification_Complete.docx')
print("הקובץ נוצר בהצלחה!")