# WhatsApp CRM MVP — הנחיות פרויקט

מוצר: AI CRM שמתחבר לוואטסאפ (ובעתיד לערוצים נוספים), מחלץ אוטומטית פרטי לקוח מהודעות, ומנהל את מחזור החיים של הליד.

## חזון המוצר (2026-08-30, מ-System_Specification_Complete.docx בשורש הפרויקט)

המוצר הוא AI Sales Copilot + CRM אוטונומי מבוסס WhatsApp, רשתות חברתיות, ותמלול
שיחות קוליות - הופך את ערוצי התקשורת של העסק למכונת מכירות ששומרת/מנהלת/מזינה
נתונים/מחזירה לחיים לידים אוטומטית. חמש בעיות/פתרונות מגדירים את המוצר (סטטוס
מיפוי נכון ל-2026-08-30 - **לעדכן כל פעם שמשהו מהמיפוי הזה משתנה**):

- **א. Speed to Lead** (מענה מיידי בתוך חלון הזמן הקריטי) - ✅ בנוי: `process_message_with_reply`/מענה אוטומטי ב-`/webhook`. ⚠️ **אין מדידה מפורשת של זמן התגובה** (SLA) - המענה קורה מיידית בפועל, אבל אין metric/תצוגה שמראה "כמה זמן עבר עד המענה".
- **ב. שמירת 100% אנשי קשר, כולל "לא רלוונטיים"**, למאגר קפוא לטיפול עתידי - ✅ בנוי: כל הודעה נכנסת יוצרת/מעדכנת כרטיס ב-`customers.json` ללא קשר לרלוונטיות.
- **ג. CRM נגיש ואוטונומי, בלי הקלדות ידניות** - ✅ בנוי: כל הדשבורד (`index.html`) - טבלה, Inbox, יומן, משוב, ציון חום, חיפוש.
- **ד. מנוע Reactivation** (סריקה מחזורית + פנייה מותאמת ב-WhatsApp) - ✅ בנוי: `reactivate.py`+`scheduler.py` (אוטומטי ברקע, פר-ורטיקל) + `/api/reactivate` (ידני, פר-ימים, עם משימת מעקב אוטומטית - ראו הפירוט למטה).
- **ה. Zero-Click Data Extraction & Call Summarization** (תמלול שיחה קולית + סיכום + חילוץ שדות אוטומטי + פנייה ב-WhatsApp להשלמת פרטים חסרים) - ✅ **נבנה ואומת בהצלחה מלאה עם קריאות OpenAI אמיתיות (2026-08-30)**: `transcription.py` (OpenAI Whisper API, `OPENAI_API_KEY` תקין ומאומת) מחובר ל-`/webhook` - הודעה קולית נכנסת (WhatsApp voice note) מתומללת אוטומטית ונכנסת לאותו זרם עיבוד כמו הודעת טקסט (`process_message_with_reply`), **וגם** לטופס הערות Click-to-Call. חילוץ שדות אוטומטי *ייעודי* מהתמלול (`transcription.extract_voice_message_fields`, gpt-4o-mini, נפרד מ-`extract_customer_info` הכללי) - `customer_name`/`property_type`/`budget` (ראו "חילוץ שדות אוטומטי מהודעה קולית עם gpt-4o-mini" למטה) - **נבדק מקצה-לקצה עם אודיו עברי אמיתי (OpenAI TTS) → Whisper אמיתי → gpt-4o-mini אמיתי → כתיבה לכרטיס, PASS מלא**. **עדיין אין**: פנייה אוטומטית ב-WhatsApp על שדות חסרים - זה נשאר לא-ממומש.
- **מנגנון חיוג רציף (sequential/power dialer)** - ✅ **נבנה (2026-08-30)** - ראו "חיוג רציף..." למטה. בוחרים לידים בטבלה → "📞 התחל חיוג רציף" → הפאנל מתקדם אוטומטית ליד-אחר-ליד, עם קישורי `tel:`/`wa.me` שעובדים גם בלי Twilio Voice מוגדר.
- **ארכיטקטורת הפעולה המלאה** (סעיף 3 במסמך): שלב 4 - "פנייה אוטומטית ב-WhatsApp להשלמת פרטים חסרים" - **עדיין לא קיים**. אין היום שום לוגיקה שמזהה "שדה חסר בכרטיס" ויוזמת הודעה לליד לבקש אותו.

## סטטוס נוכחי — חשוב לקרוא לפני הכל

המסמך הזה מגדיר חלוקת אחריות בין 6 "סוכנים" במודל Multi-Tenant & Omnichannel.
**זו הגדרת תפקידים/roadmap, לא קוד שמפעיל אוטומטית 6 סוכנים עצמאיים.** כתיבת המסמך הזה
לא יוצרת בעצמה תהליכים רצים. מה שקיים בפועל כקוד רץ כרגע:

| קובץ | תפקיד | סטטוס |
|---|---|---|
| `extract.py` | מנוע חילוץ פרטים מטקסט + שכבת CRM (`customers.json`) | ✅ עובד |
| `server.py` | שרת Webhook רב-ערוצי (whatsapp אמיתי + מענה אוטומטי, instagram/facebook placeholder) + דשבורד (`/`) + API (`/api/leads` - עם `?include_last_message=1` אופציונלי לתצוגת Inbox; `/api/messages` קריאה-בלבד; `/api/leads/status` מעדכן `lead_status`; `/api/messages/send` ו-`/api/reactivate` **שולחים בפועל**) | ✅ עובד (whatsapp), ⚠️ placeholder (שאר הערוצים) |
| `index.html` | דשבורד לידים - שתי תצוגות מתחלפות: **טבלה** (עיצוב SaaS - ראו "שדרוג עיצובי" למטה: אווטארים, מרכז בקרה בצד, בחירה מרובה, הוספת ליד) ו-**Unified Inbox** (רשימת שיחות ממוינת לפי פעילות אחרונה + חלון צ'אט קבוע לצידה) - שתיהן חולקות אותה לוגיקת צ'אט חי (`createChatController`, ראו למטה) - נטען ב-`/` | ✅ עובד |
| `whatsapp_send.py` | עטיפת שליחה משותפת ל-Twilio (`send_whatsapp_message`) - בשימוש `reactivate.py` וגם `server.py` | ✅ עובד; דורש פרטי Twilio אמיתיים ב-`.env` |
| `reactivate.py` | ניסוח הודעות חימום ללידים קרים + סיווג תגובות + שליחה בפועל דרך Twilio - גם כ-CLI (`--send`) וגם כפונקציה שקוראים לה מ-`/api/reactivate` ומ-`scheduler.py`. `get_cold_leads`/`run_reactivation_campaign` תומכים גם בסינון `days` (ברירת מחדל 30 מ-`/api/reactivate`) - "לא נוצר קשר מעל X ימים", לא רק סטטוס (2026-08-30). כל שליחה בפועל (send=True) יוצרת גם משימת מעקב ב-`calendar_tasks` | ✅ עובד |
| `scheduler.py` | **Background Scheduler Worker** - סורק תקופתית לידים קרים לפי סף זמן פר-ורטיקל (`ecommerce`/`real_estate`, מ-`contacts.csv`) ומריץ עליהם `reactivate.py`. שני מצבי הרצה: thread בתוך `server.py` (מקומי, `python server.py`), או `scheduler_worker.py` העצמאי (APScheduler, ל-production - ראו למטה) | ✅ עובד; **dry-run בלבד כברירת מחדל** - שליחה אוטומטית אמיתית דורשת `SCHEDULER_AUTO_SEND=true` מפורש ב-`.env` (כרגע לא מוגדר) |
| `Procfile` + `scheduler_worker.py` (2026-08-26) | פריסה לענן: `web` (Gunicorn, כמה workers) + `clock` (מנוע התזמון כתהליך עצמאי יחיד, APScheduler) | ✅ עובד; נבדק מקומית (לא ניתן להריץ Gunicorn עצמו על Windows - ראו הערה למטה) |
| `paths.py` (2026-08-26) | `DATA_DIR` משותף - `customers.json`/`crm_data.db`/`chat_history.txt` יכולים לשבת על דיסק מתמיד בענן (`DATA_DIR` env var); ברירת מחדל = תיקיית הפרויקט, ללא שינוי מקומי | ✅ עובד ונבדק (ברירת מחדל + override) |
| `POST /api/leads/import` + `GET /api/leads/export` (ב-`server.py`) | ייבוא לידים מ-CSV/XLSX - ממפה אוטומטית שם/טלפון/עסק/**מיקום**/מקור/סטטוס/קטגוריה/סוכן (עברית או אנגלית), upsert לפי `extract.resolve_existing_phone` (**לא** `_to_e164` ישירות - ראו "תיקון כפילויות ייבוא" למטה) + tenant דרך `import_lead`. ייצוא: CSV נקי עם כל השדות + ציון חום מחושב, כולל BOM ל-Excel. כפתורי "📥 ייבוא לידים"/"📤 ייצוא לידים" ב-`index.html` | ✅ עובד ונבדק (round-trip מלא, ראו למטה) |
| הקשחת `server.py` (2026-08-25) | משתני סביבה (PORT/HOST/FLASK_DEBUG/LOG_LEVEL/VERIFY_TWILIO_SIGNATURE), לוגים מסודרים (קונסולה + `server_error.log` מתגלגל), אימות חתימת Twilio (`X-Twilio-Signature`) ל-`/webhook`, ProxyFix מאחורי ngrok | ✅ עובד ונבדק דרך ngrok אמיתי - ראו הפירוט תחת "הקשחת production" למטה |
| `voice_call.py` (חדש) + `/api/calls/*`,`/voice/*` ב-`server.py` + טבלת `calls` ב-`db.py` + כפתור "📞 שיחה" ב-`index.html` (2026-08-27) | **Click-to-Call**: גישור שיחה (Twilio מתקשר לנציג, ואז מגשר ללקוח) + הקלטה + הערות ידניות/תמלול של הנציג → Claude מייצר תקציר שמשוקף גם ל-`customers.json`/`messages` (channel="voice"). **חיוג רציף (2026-08-30)**: בוחרים לידים בטבלה (checkbox) → "📞 התחל חיוג רציף" → הפאנל עובר אוטומטית ליד-אחר-ליד (`dialQueue`), עם קישורי `tel:`/`wa.me` מהירים בכל שלב (לא תלויים ב-Voice מוגדר). **תמלול קולי (2026-08-30)**: `transcription.py` (OpenAI Whisper API) - כפתור העלאת קובץ אודיו בתוך טופס ההערות, ממלא את אותה תיבת טקסט (לא צינור נפרד) | ✅ **צינור ה-DB/AI/UI + התור + חיבור התמלול לכרטיס נבדקו מקצה-לקצה, כולל דרך `/webhook` על הודעות קוליות נכנסות, עם תמלול+חילוץ שדות אמיתיים מול OpenAI (ראו "חילוץ שדות אוטומטי מהודעה קולית עם gpt-4o-mini" למטה)**; ⚠️ שיחת Voice אמיתית (חיוג בפועל דרך Twilio, גישור, הקלטה אמיתית) **עדיין לא נבדקה בהצלחה** - אין `AGENT_PHONE_NUMBER`/`PUBLIC_BASE_URL` מוגדרים ב-`.env` |
| `/api/tasks*` ב-`server.py` + טבלת `calendar_tasks` ב-`db.py` + כפתורי "📅 משימות"/"📅 יומן" ב-`index.html` (2026-08-30) | **מעקב/תזכורות (follow-up)** לליד - פאנל פר-ליד (פתוח מהפאנל הנגלל/Inbox) + תצוגת "יומן" גלובלית (כל המשימות הפתוחות מכל הלידים, מהקרובה ביותר) שמאפשרת קליק-דרך לצ'אט של הליד | ✅ עובד ונבדק (API + Playwright) |
| `/api/feedback*` ב-`server.py` + טבלת `system_feedback` ב-`db.py` + כפתור "💡 משוב" ב-`index.html` (2026-08-30) | **משוב על ה-CRM עצמו** (באג/רעיון) - פנימי לצוות שמפעיל את המערכת, **לא קשור ללידים** | ✅ עובד ונבדק (API + Playwright) |
| `db.compute_lead_score`/`db.search_activity` + `/api/leads?include_score=1`, `/api/leads/category`, `/api/search` ב-`server.py` (2026-08-30) | **ציון חום לליד** (1-100, מחושב-נגזר מפעילות בפועל) + **חיפוש/סינון מתקדם** (חיפוש רב-שדות כולל תוכן הודעות/הערות שיחה/משימות, סינון לפי טווח ציון וקטגוריה חופשית) | ✅ עובד ונבדק (API + Playwright) |
| `prompts.py`/`prompts.json` (חדש) + `/api/prompts*` ב-`server.py` + כפתור "🤖 תבניות וסוכנים" ב-`index.html` (2026-08-30) | **ניהול תבניות/פרומפטים** - כל חמשת הפרומפטים של סוכני ה-AI (Speed-to-Lead, חילוץ פרטים, החייאה **מותאם-ענף** עם override פר-ורטיקל, סיווג תגובה, תקציר שיחה) ניתנים לעריכה חיה מה-CRM בלי לגעת בקוד | ✅ עובד ונבדק - כולל אימות שעריכת פרומפט **משנה בפועל** את הפלט של Claude (לא רק את הקובץ) |

תפקידי QA / Sales / Scheduling / BI שמתוארים למטה **עדיין אין להם קובץ מימוש**. כדי שסוכן
יהפוך מ"תיאור תפקיד" ל"תהליך שרץ בפועל" צריך: (1) לבנות עבורו סקריפט ייעודי, ו-(2) לתזמן
אותו (Windows Task Scheduler / cron / הרצה ידנית) או להריץ אותו כ-subagent דרך Claude Code.

### מגבלות ידועות (חשוב לדעת)

- **אין עדיין Multi-Tenant אמיתי.** `customers.json` הוא קובץ יחיד וגלובלי — אין הפרדה בין
  עסקים שונים שישתמשו במערכת. אם בעתיד כמה עסקים ישתמשו באותה מערכת, צריך להוסיף
  `tenant_id`/`business_id` לכל רשומה ולבודד גישה בין עסקים. זה לא קיים כרגע.
- **Omnichannel חלקי בלבד.** WhatsApp מחובר בפועל דרך Twilio. Instagram ו-Facebook הם
  placeholder בפורמט JSON גנרי — לא מחוברים בפועל ל-Meta Graph API האמיתי.
- **⚠️ פורמטים לא-עקביים של טלפון בנתונים קיימים (התגלה 2026-08-25 בבדיקת ייבוא CSV).**
  חלק מהכרטיסים ב-`customers.json` נשמרו במפתח לפי טלפון מקומי (`0501234567`), חלק לפי
  E.164 (`+972501234567`) - **לאותו מספר אמיתי** יכולים להיות שני כרטיסים נפרדים ושונים
  (למשל דני/פרחי הכרמל תחת `default::0501234567` מול רוני/רוני קייטרינג תחת
  `default::+972501234567` - אותו מספר בדיוק, שני זהויות). `import_lead` (ומכאן גם ייבוא
  CSV/Excel) מנרמל ל-E.164 לפני upsert - כך שאם תייבאו קובץ עם ליד קיים בפורמט מקומי,
  הוא עלול "לפגוש" ולעדכן כרטיס אחר שכבר קיים ב-E.164 עבור אותו מספר, ולא את הכרטיס
  שציפיתם לו. **צריך ניקוי/איחוד נתונים ידני** של הכרטיסים הכפולים לפני שסומכים על
  זיהוי-כפילויות אוטומטי בייבוא בסביבת production; זו לא נבדקה/תוקנה אוטומטית כאן.
  **עדכון 2026-08-30:** `import_lead`/`POST /api/leads` כבר לא מנרמלים ל-E.164
  *ישירות* - `extract.resolve_existing_phone` בודק קודם אם קיים כרטיס בפורמט
  הגולמי שהתקבל, וממשיך לעדכן אותו אם כן (ראו "החייאת לידים קרים..." למטה) - זה
  מונע כפילות *חדשה* בייבוא/הוספה, אבל **לא** מאחד כרטיסים שכבר כפולים מהעבר
  (כמו דני/רוני למעלה) - הניקוי הידני עדיין נדרש.
- **⚠️ אין עדיין מספר Twilio Voice-capable מוגדר, ואין `PUBLIC_BASE_URL` חי (2026-08-27).**
  Click-to-Call (`voice_call.py`) בנוי ונבדק במלואו במצב `simulated` (בדיוק כמו חסימת
  Trial ב-WhatsApp), אבל שיחת גישור אמיתית - חיוג בפועל לנציג, TwiML ב-`/voice/connect`,
  הקלטה אמיתית, ואימות חתימת Twilio על שלושת ה-webhooks החדשים - **לא נבדקו** כאן. דורש
  מהמשתמש לרכוש מספר Voice בקונסולת Twilio, להגדיר `AGENT_PHONE_NUMBER`/
  `TWILIO_VOICE_FROM`/`PUBLIC_BASE_URL` ב-`.env`, ולחשוף URL ציבורי (ngrok/Render) - אותה
  מגבלה עקרונית כמו Gunicorn-על-Windows ואימות חתימת `/webhook` שנבדק רק דרך ngrok אמיתי.

### הקשחת production ב-server.py (2026-08-25) - מה כן ומה לא

**מה נוסף בפועל, ונבדק:**
- **משתני סביבה מלאים:** `PORT`, `HOST`, `FLASK_DEBUG`, `LOG_LEVEL`, `VERIFY_TWILIO_SIGNATURE`
  נקראים מ-`.env` עם ברירות מחדל בטוחות. `FLASK_DEBUG` בקוד הוא `false` כברירת מחדל
  (production-safe - מצב debug של Flask חושף Werkzeug debugger שמריץ קוד שרירותי) -
  ב-`.env` המקומי הוגדר `FLASK_DEBUG=true` במפורש כדי לשמר את חוויית הפיתוח.
  ייבוא נכשל (`extract.py`/`whatsapp_send.py` דורשים `ANTHROPIC_API_KEY`) נתפס עם הודעה
  ברורה ו-`sys.exit(1)` במקום traceback גולמי.
- **לוגים:** `logging` סטנדרטי - קונסולה + `server_error.log` מתגלגל (`RotatingFileHandler`,
  2MB × 5 קבצי גיבוי) שרושם WARNING/ERROR עם traceback מלא. נפרד מ-`chat_history.txt`
  (שנשאר לוג *עסקי* של תוכן הודעות, לא לוג שגיאות טכני). `@app.errorhandler(Exception)`
  תופס כל חריגה לא-מטופלת, רושם אותה, ומחזיר JSON נקי (`{"error": "..."}`) במקום דף
  שגיאה של Flask/traceback לקליינט.
- **אימות Twilio אמיתי:** `X-Twilio-Signature` מאומת מול `TwilioRequestValidator` (חלק
  מ-SDK הרשמי) לכל בקשה ל-`/webhook` בפורמט Twilio (form-encoded). **`ProxyFix` נדרש
  ונוסף** כי בלעדיו `request.url` משקף את הכתובת הפנימית (`127.0.0.1:5000`) ולא את
  הכתובת הציבורית שטוויליו חתם עליה - האימות תמיד היה נכשל בטעות. **נבדק דרך ngrok
  אמיתי**: בקשה עם חתימה מחושבת נכון על ה-URL הציבורי - התקבלה (200); בקשה בלי חתימה,
  גם דרך אותו tunnel - נדחתה (403). בדיקות ידניות (curl/פיתוח מקומי) בלי חתימת Twilio
  אמיתית יידחו אוטומטית כברירת מחדל - מגדירים `VERIFY_TWILIO_SIGNATURE=false` ב-`.env`
  זמנית אם צריך לבדוק ידנית.

**מה *לא* כוסה - "100% מוכן ל-production" הוא לא מדויק, חשוב לדעת:**
- **`/api/*` (כל נתיבי הדשבורד) עדיין ללא אימות/הרשאה בכלל** - כל מי שמגיע לכתובת יכול
  לקרוא נתוני לקוחות, לשנות סטטוסים, לשלוח הודעות ידניות, ולהריץ קמפיינים. זה לא נדרש
  במפורש בבקשה הזו (שהתמקדה ב-webhook של Twilio), אבל זו פרצה משמעותית לפריסה אמיתית.
- **`customers.json` כקובץ שטוח** לא בטוח לכתיבה מקבילית בעומס production אמיתי (race
  conditions אפשריים בין בקשות webhook/API/scheduler מקבילות) - לא נפתר כאן.
- **סודות (`TWILIO_AUTH_TOKEN` וכו') עדיין בטקסט גלוי ב-`.env`** - ל-production אמיתי
  צריך secret manager (לא מומש כאן).
- **אין HTTPS termination באפליקציה עצמה** - מניחים שהיא רצה מאחורי proxy/ngrok/load
  balancer שמטפל בזה, כמו היום.
- **אין rate limiting** על אף נתיב.

### חלון צ'אט חי ב-index.html (2026-08-26)

**מנגנון:** `pollForNewMessages()` שולפת `GET /api/messages?...&since=<timestamp ההודעה
האחרונה שכבר מוצגת>` כל 3 שניות (`LIVE_POLL_INTERVAL_MS`) כל עוד פאנל ההיסטוריה פתוח
(`startLivePolling`/`stopLivePolling` ב-`openHistory`/`closeHistory`/`closeAllPanels`).
`db.get_messages` ו-`/api/messages` תומכים עכשיו בפרמטר `since` (מחזיר רק הודעות
מאוחרות ממנו) - כדי לא לשלוף את כל ההיסטוריה מחדש בכל בדיקה. שליחה דרך הקומפוזר לא
מוסיפה בועה "אופטימית" מקומית - קוראת ל-`pollForNewMessages()` מיד אחרי שליחה מוצלחת,
ומציגה את ההודעה דרך אותו נתיב בדיוק כמו הודעות נכנסות חיות (מונע כפילות).

**למה polling ולא WebSocket/SSE:** בפריסת production (ראו Procfile למטה) Gunicorn רץ
עם כמה workers - חיבור WebSocket/SSE נעול ל-worker אחד, ואם ההודעה החדשה מגיעה
מ-webhook שנחת על worker *אחר*, לא הייתה דרך פשוטה (בלי Redis pub/sub או דומה - לא
קיים בפרויקט) להודיע לחיבור הפתוח. Polling קורא מ-`crm_data.db` המשותף בכל בקשה בנפרד
- כל worker רואה את אותם נתונים תמיד, בלי בעיית קואורדינציה בין workers (בדיוק אותה
סיבה שבגללה `scheduler_worker.py` הוא process נפרד - ראו למטה).

**נבדק אמיתי, לא רק בקוד:** נשלחה בקשת webhook חתומה אמיתית (Twilio signature תקין)
בזמן שחלון הצ'אט היה פתוח בדפדפן (Playwright) - ההודעה הנכנסת *וגם* התגובה האוטומטית
הופיעו בפאנל **בלי שום אינטראקציה עם הדף** תוך פחות ממחזור polling אחד. אומת גם
שה-polling נעצר לגמרי ברגע שסוגרים את הפאנל (0 בקשות נוספות). **תוך כדי בדיקה נמצא
ותוקן race condition אמיתי**: הקריאה המפורשת שמציגה הודעה שהמשתמש עצמו שלח יכלה
"להיבלע" בשקט אם התנגשה עם טיק polling ברקע (guard `isPolling` פשוט) - במקום להיעלם,
עכשיו היא מסמנת `pollAgainRequested` ומריצה סבב נוסף מיד; ה-fix אומת (בדיקה חוזרת:
בועה אחת בדיוק, בלי כפילות, בלי השהיה נסתרת).

### Unified Inbox (2026-08-26)

**מה נוסף:** תצוגה שנייה ב-`index.html`, לצד הטבלה הקיימת (כפתורי מעבר "📋 טבלה" /
"💬 Inbox" - `switchView`) - רשימת שיחות (`#inboxList`, ממוינת לפי `last_message_at`
יורד - השיחה עם הפעילות האחרונה למעלה, בדיוק כמו כל אפליקציית צ'אט) לצד חלון צ'אט
קבוע (לא slide-over כמו הפאנל הקיים). לכל שורה ברשימה: שם, תצוגה מקדימה של ההודעה
האחרונה (עם קידומת "את/ה: " אם היא יצאה מהמערכת), זמן, ותג סטטוס.

**Backend:** `db.get_last_message(phone, tenant_id)` חדש ב-`db.py` - שולף רק את
ההודעה האחרונה (לא כל ההיסטוריה) לכל ליד. `GET /api/leads?include_last_message=1`
(אופציונלי - ברירת המחדל בלי הפרמטר לא השתנתה, לא פוגע בטבלה הקיימת) מצרף
`last_message`/`last_message_at`/`last_message_direction` לכל ליד ברשימה.

**Frontend - refactor, לא שכפול:** הלוגיקה של "חלון צ'אט חי" (polling, composer,
race-fix `pollAgainRequested`, עדכון סטטוס) חולצה מהקוד הישן (שהיה קשור ישירות ל-
ID-ים של הפאנל הנגלל) לפונקציית מפעל `createChatController(ids)` ב-`index.html`
שמחזירה controller עם `open`/`close`/`syncStatus`. שני מופעים עצמאיים: `panelChat`
(מפעיל את `#panel` הישן - `openHistory`/`closeAllPanels` עכשיו רק wrappers דקים
סביבו) ו-`inboxChat` (מפעיל את חלון הצ'אט ב-Inbox). **אותו קוד מדויק, שתי מופעים** -
אין סיכון לסחף בין הגרסאות כמו שהיה קורה עם copy-paste. `switchView` דואג שרק
controller אחד "חי" בכל רגע - עובר ל-Inbox סוגר את הפאנל הישן (`closeAllPanels`),
ועובר לטבלה עוצר את ה-polling של ה-Inbox (`inboxChat.close()`) - כדי שלא ירוצו שני
מחזורי polling מקבילים על אותו ליד.

**נבדק אמיתי (Playwright), לא רק בקוד:**
1. טעינת שתי התצוגות + מעבר ביניהן - 0 שגיאות קונסולה.
2. פתיחת שיחה ב-Inbox, מעבר לשיחה שנייה - היסטוריה נטענת נכון בכל פעם, כולל מקרה
   "אין הודעות רשומות" לליד בלי היסטוריה.
3. **רגרסיה**: הפאנל הנגלל הישן (`#panel`) עדיין עובד זהה לחלוטין אחרי ה-refactor.
4. שליחת הודעה דרך הקומפוזר של ה-Inbox - עובד קצה-לקצה (כולל הטיפול הקיים ב-
   מגבלת Twilio Trial: הודעה נרשמת כ-`simulated`, מוצגת עם אזהרה מתאימה).
5. **זמן-אמת אמיתי**: webhook חתום (Twilio signature תקין) נשלח בזמן ששיחה פתוחה
   ב-Inbox - ההודעה הנכנסת + התגובה האוטומטית הופיעו בלי שום אינטראקציה עם הדף,
   תוך פחות ממחזור polling אחד (זהה בדיוק להתנהגות שנבדקה על הפאנל הישן).
כל נתוני הבדיקה (הודעות טסט ב-`crm_data.db`, `customers.json`, `chat_history.txt`)
נוקו בסוף.

### שדרוג עיצובי SaaS + "סוכן מטפל" + הוספת ליד ידנית (2026-08-30)

**הקשר:** בקשה נפרדת, אחרי שה-backend (יומן, ציון חום, חיפוש) כבר היה מוכן - שדרוג
`index.html` בלבד לעיצוב "SaaS מקצועית", בהשראת ממשקי ניהול נדל"ן/לידים מתקדמים.
הרוב עיצוב טהור (CSS/HTML/רינדור) שמתחבר ל-API-ים הקיימים - אבל שתי תוספות backend
זעירות היו נחוצות כדי שעמודות שהתבקשו לא יהיו "ריקות": "סוכן מטפל" ו-"➕ הוסף ליד".

**מה נוסף:**
- **פריסת עמוד חדשה** (`.dashboard-layout`): הטבלה + **מרכז בקרה** בסרגל צד (רק
  בתצוגת הטבלה - Inbox לא מושפע). מרכז הבקרה: ספירות חיות (לידים חמים/משימות
  פתוחות/משוב פתוח, כל אחת קליקבילית), ותצוגות מיני של "היום ביומן" (3 המשימות
  הקרובות) ו"משוב אחרון" (3 הפריטים החדשים ביותר) - קליק פותח את הפאנל המלא
  הרלוונטי או את הצ'אט של הליד. מתעדכן (`loadControlCenter`) אחרי כל פעולה
  שמשנה משימות/משוב, לא רק בטעינה הראשונית.
- **טבלה מחודשת** (11 עמודות): checkbox בחירה, #, שם+אווטאר עגול (ראשי תיבות,
  צבע דטרמיניסטי מה-hash של הטלפון - כך שאותו ליד תמיד מקבל אותו צבע) עם שורת
  משנה מעומעמת (עסק · מיקום), טלפון, **סוכן מטפל** (שדה חדש, input inline כמו
  קטגוריה), ערוץ, סטטוס, ציון חום, **פעילות אחרונה** (תווית יחסית - היום/אתמול/
  לפני X ימים - + תקציר ההודעה האחרונה, מ-`include_last_message=1` שעכשיו גם
  הטבלה הראשית מבקשת, לא רק ה-Inbox), קטגוריה, Tenant. **11 העמודות רחבות יותר
  מהמסך הזמין** (גם אחרי הרחבת `.page` ל-1680px) - `#tableWrap` מקבל גלילה
  אופקית פנימית משלו (לא `.table-card` - כדי לשמר את הפינות המעוגלות) - נבדק
  בפועל ש-scrollWidth>clientWidth ושגלילה בפועל חושפת את העמודות החבויות; זו
  התנהגות סטנדרטית בכלי SaaS עם הרבה עמודות, לא באג.
- **בחירה מרובה + פעולה בצע-בצוותא**: checkbox לכל שורה + "בחר הכל" בכותרת,
  פס פעולות מופיע כשיש בחירה (`#bulkBar`) עם עדכון סטטוס לכל הנבחרים בבת אחת -
  קורא ל-`/api/leads/status` הקיים בלולאה, בלי endpoint חדש.
- **"➕ הוסף ליד" (חדש):** עד עכשיו אפשר היה להוסיף ליד רק דרך ייבוא CSV/Excel
  שלם או הודעת וואטסאפ אמיתית - לא היה דרך להזין ליד בודד ידנית. `POST
  /api/leads` (server.py) עוטף את `import_lead` הקיים (אותה לוגיקת upsert
  בדיוק כמו ייבוא - בלי כפילות קוד), נפתח מפאנל `#addLeadPanel` (אותו component
  panel/overlay כמו כל שאר הפאנלים בדשבורד).
- **"סוכן מטפל" (`agent`, חדש):** שדה טקסט חופשי בכרטיס הלקוח, **בדיוק אותו
  pattern כמו `category`** (extract.update_lead_agent, `/api/leads/agent`,
  input inline בטבלה, נתמך גם בייבוא CSV) - נבחר כי העמודה נדרשה במפורש
  בבקשת העיצוב, ואי אפשר היה להציג אותה כ"אמיתית" בלי שדה נתונים אמיתי מאחוריה.

**נבדק בפועל (Playwright):** כל 11 העמודות מרונדרות נכון (כולל אווטארים, שורת
משנה, פעילות אחרונה עם snippet אמיתי לליד עם היסטוריה); בחירה מרובה מציגה/
מסתירה את פס הפעולות נכון; עריכת "סוכן מטפל" inline נשמרת ומשתקפת אחרי רענון;
הוספת ליד חדש עובדת קצה-לקצה (כולל נרמול E.164, מופיע גם ב-Inbox); שני כפתורי
"לכל..." במרכז הבקרה פותחים את הפאנלים הנכונים; **רגרסיה מלאה על תצוגת ה-Inbox**
(לא נגעו בה בכלל - אימות שהיא זהה לחלוטין). 0 שגיאות קונסולה. כל נתוני הבדיקה
(ליד לדוגמה, שדה agent לדוגמה) נוקו והאימות מול ה-API אישר חזרה ל-11 לידים בסיס.

### תמלול קולי אוטומטי בהודעות נכנסות + באג אמיתי שהתגלה ב-`.env` (2026-08-30)

**מה נוסף:** עד עכשיו התמלול (`transcription.py`) שירת רק העלאה ידנית בתוך טופס
הערות Click-to-Call. **עכשיו מחובר גם ל-`/webhook`** - כשליד שולח הודעה קולית
אמיתית בוואטסאפ (voice note), Twilio שולח `Body` **ריק** ורק שדות מדיה
(`NumMedia`/`MediaUrl0`/`MediaContentType0`). **באג אמיתי שהיה קיים לפני התיקון**:
`if not contact_id or not message_text: return Response(status=400)` ב-`/webhook`
היה דוחה הודעה כזו **מיד**, לפני שנרשם בכלל משהו - כל הודעה קולית נכנסת "נעלמה"
מהמערכת לגמרי, בלי לוג, בלי כרטיס, בלי שום דבר. זה בדיוק התסמין שהמשתמש תיאר
("הודעות נעלמות מה-Inbox").

**התיקון:** `transcription.transcribe_incoming_voice_message(request.form)` (חדש)
- בודק `NumMedia`+`MediaContentType0` מתחיל ב-`audio/`; אם כן, מוריד את המדיה
(`download_twilio_media` - Basic Auth עם TWILIO_ACCOUNT_SID/AUTH_TOKEN, בדיוק כמו
כל קריאת REST אחרת ל-Twilio; **לזיכרון בלבד, לא נכתב לדיסק** - `transcribe_audio`
שולח בייטים ישירות ל-OpenAI) ומתמלל (`transcribe_audio` הקיים - **קוד משותף, לא
כפול**, עם ההעלאה הידנית). מחזיר `None` אם זו לא הודעה קולית בכלל (מבדיל בין
"אין מה לתמלל" ל"תמלול נכשל"). **קריטי לדרישה "לא להיעלם"**: אם ההורדה/התמלול
נכשלים - מוחזר טקסט placeholder ברור (⚠️), **לא None וגם לא חריגה** - כך שההודעה
עדיין נכנסת לאותו `message_text` שממשיך בדיוק כמו הודעת טקסט רגילה (`_log_incoming_
message` + `db.log_message` + `process_message_with_reply` - **אותו צינור, בלי
קוד כפול**) - שום מסלול לא מסתיים ב"נעלם בשקט". תמלול מוצלח מסומן בקידומת "🎙️"
בהודעה כדי שהנציג יידע שזו הייתה הודעה קולית.

**🐛 באג אמיתי שהתגלה תוך כדי בדיקה - `OPENAI_API_KEY` ב-`.env` אינו מפתח תקין:**
המשתמש דיווח שעדכן את המפתח האמיתי. בדיקה עם קובץ WAV תקני אמיתי (נוצר מקומית,
לא הודעה אמיתית) נכשלה עם `UnicodeEncodeError` (לא שגיאת auth!) - אבחון (בלי
להדפיס את הערך עצמו בשום שלב): המחרוזת שנשמרה היא רק 36 תווים, מתחילה נכון
ב-"sk-pro" אבל **ממשיכה בעברית** - כנראה טקסט placeholder/תזכורת שנשמר בטעות
במקום המפתח האמיתי (מפתחות OpenAI אמיתיים הם ASCII בלבד, בפורמט `sk-proj-`
בדרך כלל 100+ תווים). **המשתמש צריך להחליף את הערך ב-.env במפתח האמיתי** מ-
platform.openai.com → API Keys - זה לא באג בקוד, קוד ה-webhook/transcription
תקין ונבדק (ראו למטה).

**נבדק בפועל, כולל תרחיש כשל מלא (המפתח הלא-תקין + URL מדיה מזויף - "worst case"
אמיתי):** webhook עם חתימת Twilio אמיתית מדומה, `NumMedia=1`+`MediaContentType0=
audio/ogg`+`Body` ריק - **ההודעה נרשמה** (לא נדחתה ב-400), עם טקסט placeholder
מסביר שהתמלול נכשל; Claude **הגיב בהקשר נכון** להודעה הקולית שלא הובנה (בלי
שהתבקש לכך במפורש - זו התנהגות אמרג'נטית טובה של `process_message_with_reply`
הקיים על טקסט חדש שלא נראה כמוהו). `/api/leads?include_last_message=1` הציג את
ההודעה נכון (סנכרון Inbox מיידי). **רגרסיה**: webhook עם הודעת טקסט רגילה (בלי
מדיה) - עדיין עובד זהה לגמרי, אין שינוי התנהגות. כל נתוני הבדיקה נוקו. **עדיין
לא נבדק**: תמלול *מוצלח* בפועל (דורש מפתח OpenAI תקין שטרם סופק).

### עריכה מלאה ללידים (Full CRUD) + עיצוב פרימיום כהה-זהב (2026-09-03)

**הקשר:** שני שדרוגים "קריטיים" שהתבקשו יחד: (1) עריכה מלאה של כל פרטי ליד
קיים מהדשבורד (כולל מחיקה), (2) שדרוג עיצובי ליוקרתי/נקי, מתאים לסוכני נדל"ן.
תוכנן ב-Plan Mode עם כמה החלטות ארכיטקטורה מפורשות מול המשתמש (ראו למטה).

**החלטות ארכיטקטורה שהתבררו מראש (חשוב לזכור):**
- **ציון חום נשאר מחושב-בלבד, לא ניתן לעריכה ידנית** - שדה נגזר
  (`db.compute_lead_score`, ראו "ציון חום לליד" למעלה) שלא נשמר בשום מקום;
  עריכה ידנית הייתה נמחקת אוטומטית בחישוב הבא ובעצם מטעה. פאנל העריכה מציג
  אותו כשורת מידע **read-only** עם הסבר, לא כשדה עריך.
- **"תגיות" לא קיבלו מנגנון נפרד** - `category` הקיים (טקסט חופשי בודד)
  משמש גם לזה, כדי לא לשכפל יכולת דומה. **"הערות" הוא שדה חדש** (`notes`,
  טקסט חופשי ארוך יותר, textarea) שלא היה קיים קודם ברמת הליד.
- **עריכת מספר טלפון נתמכת במלואה**, כולל "rekey" זהיר - טלפון הוא המפתח
  הראשי היחיד בכל המערכת (אין ID מספרי חלופי בשום מקום - ראו "עקרונות
  משותפים" למטה), ולכן שינוי טלפון לליד קיים חייב לעדכן גם את מפתח הכרטיס
  ב-`customers.json` וגם את עמודת `phone` בכל שלוש טבלאות ה-SQLite
  (`messages`/`calls`/`calendar_tasks`) - אחרת היסטוריה/הודעות/שיחות/משימות
  "נשארות מאחור" עם המספר הישן, מנותקות מהכרטיס.
- **מחיקת ליד נכללה בסקופ** (D ב-CRUD), עם מודאל אישור מפורש (לא `confirm()`
  דפדפני) - כולל מחיקה מדורגת (cascading) של כל הפעילות הטכנית הנלווית.

**קוד חדש:**
- `db.py`: `rekey_phone(old_phone, new_phone, tenant_id)` - `UPDATE` על שלוש
  הטבלאות בחיבור/commit יחיד. `delete_lead_activity(phone, tenant_id)` -
  `DELETE` משלושתן.
- `extract.py`: `update_lead_fields(...)` - שמירה מלאה מטופס עריכה (בניגוד
  ל-`import_lead`/`upsert_customer` הקיימים - כאן `""` מפורש **מוחק** שדה,
  לא רק "לא נוגעים בו כי ריק"); `rekey_lead(old_phone, new_phone, tenant_id)`
  - דוחה (`ValueError`) אם המספר החדש כבר שייך לליד *אחר* קיים (מונע מיזוג
  בטעות - מיזוג מכוון לא נתמך); `delete_lead(phone, tenant_id)` - מוחק כרטיס
  + קורא ל-`db.delete_lead_activity`. `import_lead` קיבל גם פרמטר `notes`
  חדש (עקבי לכל שאר השדות שכבר נתמכים שם).
- `server.py`: `POST /api/leads/update` (rekey קודם אם `new_phone` שונה, אח"כ
  שאר השדות תחת המפתח החדש), `POST /api/leads/delete`. `notes` נחשף מ-`GET
  /api/leads` (אותו דפוס כמו `property_type`/`budget`), ונוסף גם ל-
  `IMPORT_FIELD_ALIASES` ול-`EXPORT_COLUMNS` (ייבוא/ייצוא CSV תומכים בו
  אוטומטית, כמו כל שדה אחר).
- `index.html`: כפתור "✏️ ערוך" חדש בכל שורת טבלה (ליד "💬 היסטוריה", אותו
  תא - `.row-actions`). `#editLeadPanel` (חדש) - טופס מלא (שם/טלפון עם אזהרת
  rekey/עסק/מיקום/קטגוריה/סוכן/סטטוס/הערות + שורת ציון חום read-only) +
  כפתור "🗑️ מחק ליד". **מודאל אישור כללי חדש** (`showConfirmModal`,
  `#confirmModalOverlay`) - `Promise<boolean>`, בשימוש כרגע רק למחיקת ליד,
  אבל בנוי לשימוש חוזר עתידי (מחליף את ה-`confirm()` הדפדפני הגנרי היחיד
  שהיה בקוד, ב-`reactivateSendBtn` - זה **לא** שונה, נשאר `confirm()` פשוט
  כי לא התבקש).

**⚠️ נמצא ותוקן תוך כדי בדיקה (לא באג בקוד הסופי, רק בתרחיש הבדיקה
הראשוני):** בדיקה ידנית ראשונה של `/api/leads/update` יצרה כרטיס כפול, כי
השתמשתי בפורמט טלפון גולמי כ-lookup במקום בפורמט המנורמל שהשרת שמר בפועל
בזמן היצירה (E.164). **זה לא באג אמיתי בזרימת ה-UI** - `openEditLeadPanel`
תמיד משתמש ב-`lead.phone` המדויק מ-`allLeads` (בדיוק מה שהשרת מחזיר), אף פעם
לא בערך שהמשתמש הקליד-מחדש - כך שה-lookup תמיד תואם. תועד כאן כדי שלא
"יתגלה מחדש" בטעות בעתיד.

**נבדק בפועל (API ישיר + Playwright):**
- CRUD מלא: יצירה → עדכון שדות (כולל `notes`) → rekey לטלפון אחר לגמרי
  (הכרטיס+כל השדות עברו נכון) → ניסיון rekey שמתנגש עם ליד קיים (400,
  נדחה כראוי) → מחיקה (200) → מחיקה חוזרת של אותו ליד (404 - לא "נבלע
  בשקט") → אימות מול `/api/leads` שהכרטיס נעלם וגם ה-messages/calendar_tasks
  שהיו לו נמחקו (cascade תקין).
- Playwright: פתיחת פאנל העריכה עם נתונים קיימים ממולאים נכון (כולל שורת
  ציון חום, read-only), מודאל המחיקה נפתח/נסגר נכון (Cancel נבדק - לא מוחק
  בפועל).
- כל נתוני הבדיקה נוקו (`customers.json` חזר בדיוק ל-11 המפתחות המקוריים,
  0 שורות יתומות ב-`messages`/`calendar_tasks`/`calls` לכל מספרי הבדיקה).

**עיצוב פרימיום כהה-זהב:** לבקשת המשתמש (נבחר מפורשות מבין 3 כיוונים -
"כהה-פחם + זהב/פלטינה עמוק, luxury מודגש") - זו זהות מותג מכוונת, **לא**
"מצב כהה" מותנה-`prefers-color-scheme` כמו קודם: ה-`:root` הבסיסי *הוא*
עכשיו הפלטה הכהה-זהובה, בלי תלות בהעדפת מערכת ההפעלה; `[data-theme="light"]`
הוא גרסה בהירה-שנהב מפורשת (לבחירה ידנית בלבד, דרך `themeToggle` - פושט
מ-3 מצבים (auto/dark/light) ל-2 (default/light), כי "dark" המפורש כבר לא
שונה מברירת המחדל). טיפוגרפיה: **Frank Ruhl Libre** (serif עברי, נקרא
ב-Haaretz ופרסומים איכותיים אחרים - תחושת יוקרה/עריכתיות) לכותרות,
**Heebo** (סאן-סריף עברי מודרני, hinting טוב בהרבה מ-`system-ui` הגנרי
הקודם) לגוף הטקסט - שניהם דרך Google Fonts (`<link>` ב-`<head>`, לא
`@import` ב-CSS - טעינה מקבילה מוקדמת יותר). כפתור ראשי (`button.primary`)
עבר ממילוי כחול+טקסט לבן למילוי זהב+טקסט כהה (ניגודיות טובה יותר, ומראה
"כפתור בולט" יוקרתי יותר) עם הגבהה (`translateY`) עדינה ב-hover. רדיוסי
פינות/צללים גדלו במעט לתחושת "נשימה" (`.cc-card`/`.table-card`: 14px→18px,
`.stat-tile`: 12px→16px).

**🐛 נמצא ותוקן תוך כדי בדיקה ויזואלית:** `--status-warning` (המשמש גם לתג
"נוצר קשר" וגם לסימון "🧪 סימולציה" ב"פעילות אחרונה" - ראו הסעיף הקודם) היה
קרוב מדי בגוון ל-`--accent` (זהב) החדש - שניהם "זהב חמים", מה שהחליש את
ההבחנה החזותית שהתגלית הקודמת בנתה בכוונה. תוקן ע"י הרחקת `--status-warning`
לכתום-אש מובהק יותר (`#e08a3c` כהה, `#b5590f` בהיר) - שומר על משפחת הצבעים
החמה הכללית, אבל נשאר מובחן בבירור מהזהב הכללי של המותג. **שאר תפקידי
הצבע הסמנטיים (good/serious/critical) לא שונו במשמעות**, רק כוילו לקריאות
על משטח כהה.

**נבדק בפועל (Playwright, לא רק בקוד) - 0 שגיאות קונסולה בכל מקום:** טבלה
(מצב כהה ובהיר), פאנל עריכת ליד, מודאל מחיקה, Unified Inbox, פאנל צ'אט עם
בועות הודעה - כולם עקביים ויזואלית, קריאים, וההבחנה זהב-מותגי מול כתום-
סימולציה ברורה בשני המצבים.

### ייבוא 222 לידים (cleaned_leads.xlsx) + באג תזרים-שגיאות + בדיקת עומק E2E בקנה-מידה (2026-09-09)

**🐛 באג אמיתי שנמצא ותוקן: `import_leads.py` (סקריפט חיצוני של המשתמש) כתב
ל-טבלת SQL זרה, לא ל-`customers.json`.** המשתמש הריץ סקריפט משלו
(`import_leads.py`, לא קובץ של הפרויקט) שמזריק לידים דרך `sqlite3` ישירות
ל-`crm_data.db`. הלוגיקה שם: מחפש טבלה בשם `contacts` או `leads`, ואם אף
אחת לא קיימת - נופל ל-`tables[0]` (**הטבלה הראשונה שמופיעה**, כמעט שרירותי).
מסתבר של-`crm_data.db` יש טבלת `customers` **זרה לגמרי לארכיטקטורה** (עמודות
`phone, name, location, status` - **בלי `tenant_id`**, לא נוצרת בשום מקום
ב-`db.py` שלנו) - שם נחתו ה-222 הלידים. **קריטי: `server.py` לעולם לא קורא
מטבלת SQL בשביל לידים - מקור האמת היחיד תמיד `customers.json`** (ראו "עקרונות
משותפים" למטה) - אז גם `UPDATE tenant_id` על הטבלה הזרה לא היה משנה כלום
בדשבורד (וגם לא אפשרי טכנית - אין שם עמודת `tenant_id` בכלל). **⚠️ סיכון
נוסף בסקריפט הזה שלא מומש בפועל הפעם, אבל אמיתי:** אם אף אחת מ-`customers`/
`contacts`/`leads` לא הייתה קיימת, ה-fallback ל-`tables[0]` היה מזריק
`name`/`phone` לתוך טבלה **אחרת לגמרי** (כנראה `messages`) - סיכון קורופציה
ממשי. **לא תוקן בפועל** (זה סקריפט חיצוני של המשתמש, לא קובץ בפרויקט) - רק
דווח למשתמש.

**התיקון בפועל:** יובאו 222 הלידים מ-`cleaned_leads.xlsx` המקורי ישירות
דרך `extract.import_lead`/`resolve_existing_phone` (אותה לוגיקת upsert
בטוחה כמו `/api/leads/import`, לא מנגנון מקביל) - עם `clean_phone`/
`clean_name` (עמודות שכבר מנורמלות בקובץ) ולא `טלפון`/`שם עסק` הגולמיים,
כי `טלפון` הגולמי בפורמט עם מקפים ("054-444-8933") לא היה מתנרמל נכון
דרך `_to_e164` (לא מסיר מקפים - היה מייצר "+97254-444-8933" שבור).
תוצאה: 196 חדשים + 26 עדכונים (כפילויות בתוך הקובץ עצמו, אוחדו נכון לפי
טלפון) = 208 לידים בסה"כ (מ-12).

**🐛 באג נוסף שנמצא ותוקן תוך כדי הבדיקה (לא קשור לייבוא) - `@app.errorhandler
(Exception)` הפך כל 404/405 ל-500 + ERROR log מלאכותי.** `server.py`'s
global exception handler תפס **כל** חריגה, כולל `werkzeug.exceptions.
HTTPException` (404/405 רגילים - זרימה תקינה, למשל דפדפן שמבקש `/favicon.ico`
שלא קיים) - והחזיר להם 500 + רשם ERROR מלא עם traceback ל-`server_error.log`.
זה **בדיוק** מה שהופך סריקת לוג-שגיאות לחסרת-תועלת (כל בקשה שגויה נראית
כמו קריסת שרת אמיתית). **תוקן**: `handle_uncaught_exception` בודק קודם
`isinstance(exc, HTTPException)` - אם כן, מחזיר את קוד הסטטוס/ההודעה
האמיתיים שלו בלי לוג ERROR; רק חריגה **לא**-HTTP (קריסה אמיתית) עדיין
נלכדת, נרשמת, ומוחזרת כ-500. נבדק: `/favicon.ico` ונתיב שרירותי - שניהם
`404` נקי עכשיו (היה `500`), ו-0 רשומות ERROR חדשות נוספו ל-`server_error.log`.

**בדיקת עומק E2E בקנה-מידה של 208 לידים (Playwright + API ישיר), לפי בקשת
המשתמש - 4 מכלולים:**
1. **מונים חיים בטבלה** - שינוי סטטוס ליד → `POST /api/leads/status` →
   `loadLeads()` (מרענן טבלה+סטטיסטיקות **בלי ניווט מלא** - אומת גם דרך
   מעקב `framenavigated`, נשאר על אותו URL) - עובד נכון. **תגלית ביצועים
   אמיתית, לא באג**: ב-208 לידים, `GET /api/leads?include_score=1&
   include_last_message=1` לוקח **כ-1.5 שניות** (כל ליד פותח חיבור SQLite
   נפרד ל-`compute_lead_score`+`get_last_message` - 208×2 פתיחות חיבור) -
   בדיקה ראשונה עם המתנה של 1 שנייה "נכשלה" כי לא חיכתה מספיק; עם המתנה
   ריאלית (4 שניות) - עובד ומעודכן נכון. **לא תוקן** (לא התבקש, וזה עדיין
   מהיר מספיק לשימוש בפועל) - אבל שווה לזכור שהעומס גדל ליניארית עם כמות
   הלידים אם יתווספו עוד מאות.
2. **מנוע חיפוש/סינון** - חיפוש לפי שם (מוצא ליד עסק שיובא) ולפי טלפון
   (בפורמט E.164 המאוחסן בפועל, לא הפורמט הגולמי-עם-מקפים מהאקסל) - עובד.
   סינון סטטוס "__new" (208-ה לידים שיובאו לא קיבלו `lead_status` מפורש -
   נופלים כברירת מחדל ל"חדש") - עובד. 0 שגיאות קונסולה.
3. **צינור תקשורת** - פתיחת צ'אט לליד עסק שיובא (למשל "CIG") מציגה את השם
   הנכון בכותרת הפאנל. **סימולציה מלאה של מנוע ההחייאה** (ליד בדיקה מבודד
   ב-`contacts.csv`): `eligible-count` → תצוגה מקדימה (מוודא שהשם "בדיקת
   E2E" **לא** דלף להודעה - `extract_safe_first_name` זיהה אותו כלא-בטוח
   ופתח בברכה ניטרלית, בדיוק כמתוכנן) → שליחה אמיתית (`send:true`) - חזרה
   מיידית (0.02 שנייה, לא חוסמת) עם `batch_id`, polling עד `completed`,
   `result:"simulated"` (חשבון Twilio עדיין Trial) - כל השרשרת מ-2026-09-06
   עדיין תקינה במלואה אחרי הייבוא הגדול.
4. **סריקת יומנים** - `server_error.log`: 0 שורות ERROR חדשות לאורך כל
   סבב הבדיקות (אחרי תיקון הבאג למעלה). `ngrok_err.log`: ריק (0 שורות) -
   אין טאנל ngrok פעיל כרגע, אין מה לבדוק שם מעבר לזה.

כל נתוני הבדיקה (ליד הבדיקה המבודד, הודעות/משימות/batch שלו) נוקו,
`contacts.csv` שוחזר ל-8 השורות המקוריות, אומת חזרה ל-208 לידים בסיס.

### כרטיס "🔥 החייאת לידים" במרכז הבקרה (2026-09-06)

**הקשר:** הצעד האחרון בסבב ההחייאה - להביא את מה שכבר נבנה (כמות לידים קרים,
קמפיין מבוקר-קצב ברקע עם מעקב חי) **לחזית הדשבורד**, לא רק בתוך פאנל שצריך
לפתוח. "כרטיס בולט" בסגנון הפחם-זהב הקיים, שמחובר לאותו קוד קיים בדיוק - לא
מנגנון מקביל.

**🐛 נמצא ותוקן תוך כדי בדיקה (לא קשור לפיצ'ר הזה עצמו):** שני מתוך ארבעת
ה-`<select>` שקיבלו את אפשרות "החייאת לידים" בסבב קודם (`#statusFilter`,
`#bulkStatusSelect`) הכילו את ה-`<option>` **פעמיים** - שרידי `replace_all`
לא-מדויק מהעריכה ההיא. תוקן (הוסרה הכפילות בשניהם); `#addLeadStatus`/
`#editLeadStatus` היו כבר תקינים.

**נתיב API חדש - קריא-בלבד וזול בכוונה:** `GET /api/reactivate/eligible-count`
(`server.py`) - קורא ל-`reactivate.get_cold_leads` בלבד (בדיקת סטטוס/תאריך),
**לא** ל-`run_reactivation_campaign`/`generate_outreach_message` - קריאת
Claude אמיתית לכל ליד קר רק כדי להציג מספר על כל טעינת דשבורד הייתה בזבוז
תקציב API ואיטית; `get_cold_leads` עצמה זולה וכבר קיימת. מחזיר
`{tenant_id, days, eligible_count}`.

**כרטיס חדש במרכז הבקרה (`index.html`, `.cc-card.cc-card-reactivation`,
בין "🎯 מרכז בקרה" ל"📅 היום ביומן"):**
- **מצב "רגוע"** - מספר גדול בגרדיאנט-זהב (אותה שפה טיפוגרפית כמו `.stat-tile
  .value`) עם כמות הלידים הזמינים כרגע, כיתוב "לא נוצר קשר מעל X ימים ·
  tenant" (מ-`tenant_settings`, אותו מקור בדיוק כמו פאנל ההחייאה), וכפתור
  ראשי "🔥 הפעל קמפיין".
- **מצב "רץ ברקע"** - אם קיים batch עם `status=="running"` (נבדק מול `GET
  /api/reactivate/batches` - **אותה טבלה בדיוק** שפאנל ההחייאה כבר עוקב
  אחריה, לא state מקביל): המספר הגדול הופך ל-X/Y (עם גוון status-warning
  במקום זהב - מאותת "מצב זמני", לא הכמות הסטטית הרגילה), שורת מונים
  (נשלחו/סימולציה/נכשלו), וכפתור "👁️ צפה בהתקדמות". **הכרטיס ממשיך לעקוב
  (poll כל 4 שניות) עצמאית**, גם אם פאנל ההחייאה סגור - זה בדיוק מה שהופך
  אותו ל"מעקב בזמן אמת" אמיתי מהדשבורד הראשי, לא רק בתוך פאנל שצריך לפתוח.
- **הכפתור בשני המצבים פותח את פאנל ההחייאה הקיים** (`document.getElementById(
  "reactivateBtn").click()`) - **לא עוקף את חובת התצוגה-המקדימה לפני שליחה**
  (מדיניות הבטיחות ב-CLAUDE.md); "בולט ומחובר" לא אומר "מסלול-קיצור מסוכן".
- מתעדכן: אחרי `loadControlCenter()` (כל נקודות הקריאה הקיימות - טעינה
  ראשונית, אחרי פעולות משימות/משוב/לידים), אחרי שינוי `tenantFilter`, מיד
  אחרי לחיצה על "שמור כברירת מחדל" בפאנל (סף הימים משתנה → הכמות משתנה),
  ומיד אחרי לחיצה על "שלח בפועל" בפאנל (עובר ל"רץ ברקע" בלי לחכות למחזור
  `loadControlCenter` הבא).

**נבדק בפועל (Playwright + API ישיר):** `GET /api/reactivate/eligible-count`
מחזיר ספירה נכונה (אומת גם דרך בדיקה ישירה של סטטוסי כל הלידים ב-tenant
"default" - כולם `contacted`/`reactivation` מריצות קודמות חוץ מ-2 חריגים
תקינים: שירה=`ig_9988` לא בקובץ אנשי הקשר בכלל, ירין גבאי=ליד אמיתי חדש לא
בקובץ אנשי הקשר). מסך מלא (לא screenshot מבודד של האלמנט - `backdrop-filter`
לא מצטלם נכון מבודד ב-Playwright, לא קשור לתקינות בעמוד עצמו): הכרטיס מוצג
נכון עם המספר בגרדיאנט-זהב, הכיתובים, והכפתור. מחזור מלא עם ליד בדיקה מבודד:
מצב רגוע (1 זמין) → לחיצה על "🔥 הפעל קמפיין" → תצוגה מקדימה → שליחה → סגירת
הפאנל (השליחה ברקע ממשיכה) → הכרטיס עצמו (בלי שהפאנל פתוח!) עבר למצב
"⏳ קמפיין רץ ברקע" ואז חזרה למצב רגוע (0 זמין, כי הליד היחיד טופל) - **בדיוק
ה-"מעקב בזמן אמת" שהתבקש**. 0 שגיאות קונסולה בכל שלב, `server_err.log` נקי
לגמרי (0 שורות ERROR/Traceback) לאורך כל סבב הבדיקות. כל נתוני הבדיקה נוקו,
`contacts.csv` שוחזר, חזרה ל-12 לידים בסיס.

### מנוע שליחה מבוקר (Rate Limiting) + הרצה ברקע עם מעקב חי (2026-09-06)

**הקשר:** קמפיין `--send`/`/api/reactivate` שלח הודעות לכל הלידים הקרים בלולאה
צפופה, בלי שום השהיה ביניהן - קצב לא-טבעי שנראה כמו ספאם/בוט מול Twilio/
WhatsApp. התבקש: השהיה רנדומלית 15-30 שניות בין הודעה להודעה, שהריצה תישאר
"יציבה ברקע", ותיעוד ברור של סטטוס השליחה **בטבלה** (SQLite, לא רק בתשובת
ה-API הרגעית שהייתה נעלמת ברגע שהבקשה הסתיימה).

**למה זה חייב לרוץ ברקע, לא רק "להוסיף sleep":** עם השהיה של עד 30 שניות בין
הודעות, קמפיין של כמה לידים יכול לארוך דקות - יותר מדי זמן להחזיק בקשת HTTP
פתוחה מול הדשבורד (טיימאאוט בדפדפן/שרת, וחוויית משתמש גרועה - מסך תקוע).
**נדרש שינוי ארכיטקטורה, לא רק קבוע-זמן חדש** - `send=True` דרך `/api/reactivate`
עובר עכשיו להרצה ב-thread נפרד (`threading.Thread(daemon=True)`, אותו דפוס
בדיוק כמו `scheduler.py`), עם טבלת מעקב ב-DB שהדשבורד סורק (polling) כדי לדעת
מה קורה. **CLI (`python reactivate.py --send`) נשאר סינכרוני כרגיל** - זה
סקריפט טרמינל, לא בקשת HTTP; ה-`sleep` עצמו רץ שם ישירות (זה כבר "רקע" מבחינת
המשתמש - הטרמינל תפוס אבל זה בסדר לסקריפט CLI).

**קוד חדש:**
- `reactivate.py`: `RATE_LIMIT_MIN_SECONDS`/`RATE_LIMIT_MAX_SECONDS` = 15/30.
  `run_reactivation_campaign` מקבל `batch_id` אופציונלי חדש; אם `send=True`
  ולא סופק - נוצר batch אוטומטית (כך גם הרצת CLI תתועד, לא רק זו מהדשבורד).
  `time.sleep(random.uniform(15, 30))` בין הודעה להודעה - **לא** אחרי האחרונה
  (אין טעם להשהות כשאין עוד מה לשלוח), ו**לא** תלוי אם השליחה הצליחה/נכשלה
  (הקצב קבוע בין ניסיונות, לא רק בין הצלחות - אחרת כשל חוזר בלי rate limiting
  היה יכול "לברוח" מהמנגנון). כל תוצאה (`sent`/`simulated`/`error`) נכתבת
  ל-DB מיידית דרך `db.add_batch_item`, לא רק בסוף הריצה. **יציבות**: כל
  הלולאה עטופה ב-try/except שמסמן את ה-batch כ-`failed` (ולא משאיר אותו
  תקוע על `running` לנצח) אם קורה כשל בלתי-צפוי (למשל קריסת קריאת Claude),
  ואז מזריק את החריגה הלאה.
- `db.py`: שתי טבלאות חדשות - `reactivation_batches` (רמת-ריצה: `status`
  running/completed/failed, `total_leads`, `sent_count`/`simulated_count`/
  `error_count` מצטברים) ו-`reactivation_batch_items` (רמת-ליד בתוך ריצה:
  `phone`/`name`/`message`/`result`/`error`/`created_at`) - אותו יחס בדיוק
  כמו `calls`/`calendar_tasks` (טבלת-ריצה + טבלת-פריטים, `id` אוטומטי, בלי
  ID מספרי-לקוח בשום מקום). `create_reactivation_batch`/`add_batch_item`
  (מעדכן גם את המונים המצטברים על ה-batch, כדי שלא יהיה צורך לספור מחדש את
  כל השורות בכל poll)/`finish_reactivation_batch`/`get_reactivation_batch`
  (batch + כל הפריטים, למסך חי)/`get_reactivation_batches` (רשימת היסטוריה
  קצרה, בלי הפריטים המלאים).
- `server.py`: `/api/reactivate` עם `send=True` כבר לא מריץ סינכרונית - סופר
  קודם כמה לידים קרים יש (`get_cold_leads`, בלי לשלוח כלום), יוצר batch, מתחיל
  `Thread` נפרד שמריץ את הקמפיין האמיתי, ומחזיר מיד `{batch_id, total_leads,
  status:"started"}`. `send=False` (תצוגה מקדימה) **נשאר סינכרוני כמו קודם** -
  מהיר, בלי קריאות Twilio/השהיות, אין סיבה לשנות. נתיבים חדשים: `GET
  /api/reactivate/batches/<id>` (מצב חי לfollow-up polling), `GET
  /api/reactivate/batches` (היסטוריית ריצות אחרונות).
- `index.html`: פס סטטוס חדש (`#reactivateBatchStatus`, **מחוץ** ל-
  `#reactivateBody` בכוונה - כדי לא להימחק כשהתצוגה המקדימה מתרעננת) - כתום
  "⏳ שולח... X מתוך Y" בזמן ריצה, ירוק "✅ הסתיים" / אדום "❌ נכשל" בסיום.
  `pollReactivateBatch(batchId)` (setTimeout רקורסיבי, כל 4 שניות, נעצר
  לבד כש-`status` אינו `running`) ממלא בהדרגה את שורות "✅ נשלח"/"🧪 סימולציה"/
  "❌ נכשל" בכרטיסי הלידים שכבר הוצגו בתצוגה המקדימה - **לא** כותב מחדש
  כרטיסים שכבר מולאו (`dataset.filled`), רק את מה שהתעדכן. סגירת הפאנל
  עוצרת את ה-polling (לא את השליחה עצמה בשרת - הריצה ב-thread ממשיכה גם
  אם אף אחד לא מסתכל). כפתור השליחה נשאר מנוטרל לאורך כל הריצה (אותה
  התנהגות "one-shot" כמו קודם - תצוגה מקדימה חדשה נדרשת כדי לשלוח שוב).

**נבדק בפועל, כולל תזמון אמיתי (לא מדומה):** `POST /api/reactivate` עם
`send:true` חזר תוך 0.1 שניות (לא חוסם) עם `batch_id`. Polling הראה התקדמות
חיה אמיתית - פריט ראשון הופיע אחרי ~8 שניות (ניסוח ההודעה + ניסיון שליחה),
פריט שני רק אחרי שהזמן שחלף בין יצירת שני הפריטים ב-DB עמד על 34.3 שניות
(עקבי עם 15-30 שניות השהיה + זמן ניסוח ההודעה הבאה) - `status` עבר נכון
running→completed. השרת עצמו (thread הבקשות של Flask) נשאר רספונסיבי לגמרי
לאורך כל הריצה - כל בקשת poll נענתה מיידית, לא נחסמה ע"י ה-thread ברקע.
CRM עודכן נכון (`lead_status: reactivation` לשני הלידים). Playwright: תצוגה
מקדימה → שליחה → פס הסטטוס עובר כתום→ירוק, כרטיס הליד מציג "🧪 סימולציה"
בכתום - 0 שגיאות קונסולה בכל שלב. כל נתוני הבדיקה (כרטיסים, הודעות, משימות,
וגם שורות ה-batch/batch_items עצמן) נוקו; `server_err.log` נקי לגמרי מכל
traceback/שגיאה לאורך שתי הרצות הבדיקה.

### מנוע החייאה: סטטוס ייעודי, מרווח-זמן פר-tenant, ושם פרטי בטוח (2026-09-06)

**הקשר:** שני רכיבים שהתבקשו יחד להעמקת מנוע ההחייאה: (1) סטטוס ליד ייעודי
+ מרווח-זמן (`last_contacted_at`) הניתן להגדרה **פר-עסק** ("קמעונאות מול
נדל\"ן דורשים חוקיות שונה"), (2) מנגנון חילוץ שם-פרטי בטוח עם הגנת-נפילה
(fallback) למניעת הודעות רובוטיות/מביכות.

**סטטוס "החייאת לידים" (`reactivation`, חדש):** נוסף ל-`VALID_LEAD_STATUSES`
(`server.py`) ול-`STATUS_META`/`STATUS_OPTIONS`/כל 4 רשימות ה-`<select>`
הסטטיות ב-`index.html` (מסנן טבלה, בחירה-מרובה, הוספת/עריכת ליד). תג/select
בצבע `--accent` (זהב-מותגי) - נבדל בכוונה מ-good/warning/critical הקיימים.
`reactivate.run_reactivation_campaign` מעדכן עכשיו לסטטוס `"reactivation"`
**במקום** `"contacted"` הכללי כשליד נשלחת אליו הודעת החייאה (אמיתית או
מדומה-Trial) - כך "פנינו מחדש ליד קר" נשאר גלוי ונבדל מ"נוצר קשר" רגיל.
`ALREADY_HANDLED_STATUSES` כולל גם אותו, כדי שליד לא יטופל שוב מיד בהרצה הבאה.

**מרווח-זמן פר-tenant (חדש - `tenant_settings.py`/`tenant_settings.json`):**
אותו דפוס בדיוק כמו `prompts.py`/`prompts.json` (JSON = מקור אמת בזמן ריצה,
קבוע בקוד = רשת ביטחון). `get_reactivation_days(tenant_id)`/
`set_reactivation_days(tenant_id, days)` - ברירת מחדל 30 יום לכל tenant
שלא הגדיר ערך משלו. **נחשף רק דרך `/api/reactivate` (הדשבורד)** - `days`
שלא סופק במפורש נופל ל-`tenant_settings.get_reactivation_days(tenant_id)`
במקום קבוע `DEFAULT_COLD_DAYS` גלובלי-קבוע כמו קודם. **`reactivate.py`'s CLI
(`python reactivate.py`, ו-`scheduler.py`'s סף פר-ורטיקל האוטומטי) נשארו
ללא שינוי** - `get_cold_leads`'s `days=None` עדיין מייצג "סינון-סטטוס-בלבד"
כפי שתועד ב"החייאת לידים קרים לפי ימים" למעלה; זו קריאת-בחירה מכוונת -
תוספת תצורה חדשה לא אמורה לשנות בשקט התנהגות אוטומטית קיימת ומתועדת.
`reactivate.DEFAULT_COLD_DAYS` הפך ל-alias של `tenant_settings.DEFAULT_REACTIVATION_DAYS`
(מקור-אמת יחיד למספר 30, לא שני קבועים נפרדים שעלולים להתפצל בעתיד).

**נתיבי API חדשים:** `GET /api/tenant-settings?tenant_id=X` (קריאה),
`POST /api/tenant-settings` (`{tenant_id, reactivation_days}`, מאמת `>=0`
שלם, 400 אחרת). **UI:** תיבת "לא נוצר קשר מעל __ ימים" בפאנל ההחייאה נטענת
עכשיו מהערך השמור של ה-tenant הנוכחי (`tenantFilter`, נופל ל-"default")
בפתיחת הפאנל, עם כפתור "💾 שמור כברירת מחדל" חדש ליד השדה ו-hint שמראה
איזה tenant בדיוק מושפע. גם קריאות התצוגה-המקדימה/השליחה ל-`/api/reactivate`
עברו להעביר `tenant_id` מפורש (**היה חסר לגמרי קודם** - הפאנל תמיד פעל על
`"default"` מובלע בשרת, בלי קשר לאיזה tenant נבחר בטבלה הראשית; עכשיו זה
עקבי, ונדרש בפועל כדי שההגדרה הפר-tenant תהיה משמעותית).

**מנוע שם-פרטי בטוח (`extract.extract_safe_first_name`, חדש):** מנקה רווחים
כפולים/טאבים, מחלץ מילה ראשונה כשם פרטי, עם תמיכה בקידומות מרובות-מילים
נפוצות בעברית (`_COMPOUND_FIRST_NAME_PREFIXES = {בן,בת,אבי,אבו}` - "בת שבע"
נשאר "בת שבע", לא רק "בת"). **הגנת-נפילה משולשת** - מחזיר `None` (לא מנחש/
ממציא שם) אם השדה ריק, מכיל ספרה כלשהי, או מכיל (substring, לא רק התאמה
מדויקת) מילת-מפתח גנרית (`_GENERIC_NAME_KEYWORDS` - "לקוח"/"מתעניין"/"ליד"/
"guest"/"test" וכו') - כדי לתפוס גם "לקוח פוטנציאלי"/"מתעניין חדש".

**שילוב ב-`reactivate.generate_outreach_message`:** ה-placeholder בפרומפט
(`OUTREACH_PROMPT` בקוד **וגם** `reactivation_outreach.template` השמור ב-
`prompts.json` - שניהם עודכנו יחד, אחרת `.format()` היה נכשל ב-`KeyError`
ברגע שהקוד משתמש בשם-placeholder חדש) הוחלף מ-`{name}` הגולמי ל-`{name_line}`
מחושב: אם יש שם בטוח - "שם הלקוח: X - פתח בפנייה אישית בשמו"; אם אין - הנחיה
מפורשת ל-Claude "אין שם בטוח לשימוש... פתח בברכה ניטרלית (למשל \"היי, מה
שלומך?\")" - **לא** מטמיע ישירות מחרוזת קבועה, נותן ל-Claude לנסח באופן טבעי
בהקשר, רק מכוון את הבחירה. `vertical_overrides` (עדיין ריק ב-prompts.json)
לא נגע - אין מה למגר שם.

**נבדק בפועל:**
- 14 מקרי-קצה ל-`extract_safe_first_name` (שם רגיל/מרובה-מילים/קידומת-מורכבת/
  ריק/None/רווחים-בלבד/מכיל-ספרה/מילת-מפתח בכל וריאציה) - כולם עברו.
- `GET`/`POST /api/tenant-settings` - ברירת מחדל 30 ל-tenant חדש, שמירה+קריאה-
  חוזרת של ערך מותאם, ולידציה (מספר שלילי/לא-מספרי → 400) - כולם עברו.
- `reactivation` כסטטוס תקף דרך `/api/leads/status` (200, נשמר נכון), וסטטוס
  לא-קיים עדיין נדחה (400) - רגרסיה תקינה.
- **קריאה אמיתית ל-Claude** (לא מוק) עם `contacts.csv` מבודד זמנית לשתי שורות -
  "לקוח 123" (שם גנרי+ספרה) הפיק "היי, מה שלומך? עבר קצת זמן..." (**בלי שם
  בכלל**, בדיוק כמבוקש), "דוד לוי" הפיק "היי דוד, מה שלומך?..." (שם פרטי נכון
  מוזרק). זו הייתה `send:false` (תצוגה מקדימה) - לא נוצרו/שונו כרטיסים, אומת
  ישירות ש-0 מפתחות דלפו ל-`customers.json`. `contacts.csv` שוחזר ל-8 הלידים.
- Playwright: אפשרות "החייאת לידים" מופיעה בפאנל העריכה ונשמרת עם תג זהב
  נכון בטבלה; פאנל ההחייאה מציג hint נכון ("ברירת המחדל הנוכחית עבור
  'default': 30 ימים..."); 0 שגיאות קונסולה. `server_err.log` נקי לגמרי
  לאורך כל הבדיקה - אין שגיאה/traceback אחת.
- **לא נוגע:** תוך כדי בדיקה נמצא ליד אמיתי נוסף שלא נוצר ע"י אף בדיקה כאן
  ("ירין גבאי", `+972544491929`) - כנראה הודעת webhook אמיתית שהתקבלה תוך
  כדי העבודה. **לא נמחק/שונה** - זה נתון אמיתי, לא שיירי בדיקה; בסיס הלידים
  עלה בעקבותיו מ-11 ל-12 (תקין, לא רגרסיה).

### שדרוג "Luxury Dark Theme" v2 - Glassmorphism (2026-09-06)

**הקשר:** המשתמש חש שהעיצוב הכהה-זהוב מהסעיף הקודם "פשוט ושטוח מדי" - ביקש
שדרוג רציני יותר: glassmorphism עם גבולות נוצצים דקים, טיפוגרפיה חדה יותר
לכותרות/מספרי-סיכום, וכפתורים/טבלה עם אפקטי hover וצללים עמוקים יותר - "מוצר
SaaS במיליון דולר". זה שדרוג CSS/HTML טהור על גבי אותה זהות-מותג (כהה-זהב)
שכבר נקבעה בסעיף הקודם - לא שינוי כיוון, אלא העמקה שלו.

**מחלקת `.glass` חדשה (reusable, לא כפילות קוד בין רכיבים):** מוחלת על
`.cc-card`/`.stat-tile`/`.table-card`/`.panel`/`.modal-box` - במקום `background`
אטום שהיה לכל אחד בנפרד. `.glass` נותנת: (1) `background: var(--glass-bg)`
שקוף-חלקית (לא אטום) + `backdrop-filter: blur(20px) saturate(150%)` - רואים
את הילת-הרקע (`--page-bg-image`, ראו למטה) דרך הכרטיס, זו המהות של
"glassmorphism"; (2) טבעת-הילה זהב/פלטינה 1px דרך `::before` עם מיסוך
(`mask-composite: exclude` על שכבת `content-box`+`inset:0`) - לא `border`
רגיל, כי גרדיאנט לא עובד ישירות על `border-color`. **`.panel`/`.modal-box`
מקבלים גרסה אטומה יותר** (`--glass-bg-solid`, ~95% אטימות במקום ~65%) דרך
`.panel.glass, .modal-box.glass { background: var(--glass-bg-solid) }` -
תוכן צפוף וממושך (צ'אט, טפסים) דורש קריאות גבוהה יותר מ"זכוכית אמיתית";
כרטיסי סיכום/טבלה (חטיפה חזותית קצרה) יכולים להרשות לעצמם שקיפות גבוהה יותר.

**רקע רב-שכבתי:** `body` עבר מ-`background: var(--surface-2)` (צבע שטוח) ל-
`background: var(--page-bg-image), var(--surface-2)` - שלוש הילות `radial-gradient`
זהובות/פלטינה עדינות (`--page-bg-image`, מוגדר לכל ערכת-נושא בנפרד) מעל
ה-surface הבסיסי, עם `background-attachment: fixed` (ההילות "עומדות" ביחס
למסך, לא זזות עם הגלילה - תחושת עומק אטמוספרי יציבה מתחת לכרטיסי הזכוכית).

**טיפוגרפיה חדה יותר:** `header.top h1` ("CRM לידים") וגם `.stat-tile .value`
(מספרי הסיכום) עברו לגרדיאנט-טקסט (`background-clip: text` +
`-webkit-text-fill-color: transparent`) - לא צבע טקסט שטוח. חשוב: מספרי
הסיכום משתמשים ב-`var(--tile-color, var(--accent))` כנקודת הגרדיאנט השנייה
(לא זהב קבוע) - כך שקידוד-הצבעים הסמנטי הקיים (חם=ירוק/נוצר קשר=כתום/לא
רלוונטי=אדום, ראו `renderStats`) נשמר, רק "מוקרן" גם על הטיפוגרפיה, לא רק
על מסגרת האריח. `.cc-card h3` (כותרות מיני בסרגל הצד - "מרכז בקרה"/"היום
ביומן"/"משוב אחרון") **יצא מהכלל הכללי** של `h1,h2,h3{font-family:Frank
Ruhl Libre}` וקיבל טיפול נפרד: sans-serif אחיד (Heebo, יורש), `uppercase`,
`letter-spacing:1.4px`, צבע `--accent` - "תווית פרימיום" (eyebrow label)
בסגנון עיתונאי-יוקרתי, כי tracking+uppercase עם serif נראה מגושם.

**כפתורים:** `button.primary` עבר ממילוי-זהב שטוח לגרדיאנט תלת-גוני
(`#e2c07f`→`--accent`→`#b78a45`) עם `inset 0 1px 0 rgba(255,255,255,.35)`
(הברקה עליונה - מראה "כפתור זכוכית מלוטש") וצל כפול עמוק יותר; hover מגביה
יותר (`translateY(-2px)`) עם צל מוגבר. כפתורים רגילים: hover עבר מ"שינוי
background שטוח" (שהיה `background: var(--surface-3)`, זהה לרקע הבסיס אחרי
שדרוג-משנה שכבר הוחל - כלומר לא היה נראה בפועל, נמצא ותוקן) להילת accent
שקופה-חלקית (`box-shadow` נוסף) + הגבהה - "hover חלק ומואר" כפי שהתבקש.

**טבלה:** כותרות עמודה (`thead th`) עברו מ-`text-secondary` רגיל ל-`--accent`,
`uppercase`, `letter-spacing:0.8px` - עקבי עם שפת ה"תווית פרימיום" של
`.cc-card h3`. hover שורה עבר מ-`background: var(--surface-3)` (מילוי אפור
שטוח) להילת accent שקופה-חלקית (`rgba(205,164,94,0.07)`) - עקבי עם שפת
ה-glass הכללית. אווטארים קיבלו טבעת-זהב עדינה (`box-shadow` כפול: טבעת +
צל).

**נבדק בפועל (Playwright, מצב כהה ברירת-מחדל + toggle לבהיר) - 0 שגיאות
קונסולה בשניהם:** טבלה מלאה, פאנל צ'אט פתוח, שני מצבי הערכת-נושא - הזכוכית
(blur+שקיפות+טבעת-הילה) נראית נכון בשני המצבים, המספרים/כותרות עם גרדיאנט
קריאים וברורים, כפתור "➕ הוסף ליד" (`primary`) מציג את ההברקה/הצל המוגברים
כמצופה. לא בוצע שינוי קוד Python בסעיף הזה בכלל - CSS/HTML בלבד ב-`index.html`,
לכן לא נדרשה בדיקת נתונים/ניקוי (אין מוטציית state).

### סימון "פעילות אחרונה" מדומה בטבלה וב-Inbox (2026-08-31)

**הקשר:** אחרי הרצת קמפיין החייאה מלא במצב סימולציה (למעלה), נבדק ויזואלית איך
זה נראה בדשבורד - והתגלה פער אמיתי: עמודת "פעילות אחרונה" בטבלה, וגם תצוגה
מקדימה ברשימת ה-Inbox, הציגו את ההודעה המדומה **בדיוק כמו הודעה שנשלחה
באמת** - בלי שום סימון. מי שהיה מסתכל בדשבורד (בלי לפתוח את הצ'אט המלא, ששם
`renderBubble` **כבר** מסמן `simulated` נכון - מסגרת מקווקוות כתומה + "⚠️
סימולציה") היה יכול לחשוב בטעות שהלקוח קיבל את ההודעה בפועל.

**הסיבה השורשית:** `db.get_last_message` כבר החזיר `simulated` מה-`messages`
table, אבל `GET /api/leads?include_last_message=1` ב-`server.py` **זרק את
זה בדרך** - שלף רק `message`/`timestamp`/`direction` מהתוצאה, לא `simulated`.

**התיקון:** `server.py` מוסיף עכשיו `last_message_simulated` לכל ליד (אותו
דפוס `include_last_message` קיים, שדה נוסף בלבד). `index.html`:
`lastActivityCellHtml` (עמודת הטבלה) ו-`renderInboxList` (תצוגה מקדימה
ב-Inbox) מוסיפים קידומת "🧪 " וצבע כתום (`--status-warning`, אותה שפה חזותית
בדיוק כמו בועת ה-simulated בצ'אט) כשה-flag דלוק, + `title` tooltip מסביר.
**לא נוגע** ב-`renderBubble` עצמו (כבר היה תקין).

**נבדק בפועל (Playwright, כולל zoom על האלמנט לוודא שה-אמוג'י 🧪 באמת מוצג
ולא רק תו לא-מזוהה):** 5 הלידים מהקמפיין הריאלי (למעלה) מוצגים נכון בכתום
עם 🧪 גם בטבלה וגם ב-Inbox, לידים בלי פעילות מדומה (כמו דני/דני כהן) נשארו
ללא שינוי. 0 שגיאות קונסולה.

### מצב סימולציה ל-`reactivate.py --send` בחשבון Trial (2026-08-30/31)

**הקשר:** בניסיון שליחת אמת (`reactivate.py --send`) התגלה שחשבון ה-Twilio עדיין
ב-Trial, וכל שליחה ללקוח שלא אומת (`Verified Caller ID` בקונסולת Twilio - **דרישה
נפרדת לגמרי מהצטרפות ל-WhatsApp Sandbox עם `join <קוד>`**) נדחית עם `422` וקוד
Twilio `572002` ("No Twilio trial phone number is assigned for messaging to
this destination number. Please add the 'to' number as a verified recipient").
**נבדק ונשלל לחלוטין ניסיון לעקוף את זה
דרך ה-API** (`client.validation_requests.create` - הפונקציה הרשמית של Twilio
להוספת Verified Caller ID) - נכשל עם קוד Twilio **20003** ("This feature is
not available on a Trial account") - **אותה מדיניות אנטי-ספאם בדיוק, אכופה
זהה גם ב-API וגם באתר** - אין עקיפה טכנית, רק שדרוג חשבון אמיתי (הוספת אמצעי
תשלום) מסיר את המגבלה.

**מה נוסף (בהסכמת המשתמש - "נשאר ב-Trial", מצב סימולציה מפורש לבדיקת הצינור
המלא בלי לצאת מ-Trial):** `whatsapp_send.is_trial_restriction(exc)` (חדש,
**רופרקטור** - הועבר מ-`server.py`'s `_is_trial_restriction` הפרטית ל-מודול
המשותף `whatsapp_send.py`, כדי ש-`reactivate.py` יוכל להשתמש באותה זיהוי בדיוק
בלי לשכפל קוד/לייבא מ-`server.py` - server.py מייבא אותה עכשיו במקום להגדיר
מקומית). `reactivate.run_reactivation_campaign` (בתוך `if send:`) עכשיו תופס
`is_trial_restriction(exc)` בנפרד משגיאה אמיתית אחרת: `entry["simulated"] = True`
(שדה חדש ב-entry, לצד `sent`/`error` הקיימים) - ו**ממשיך** לעדכן סטטוס ל-
`contacted`, לרשום הודעה ב-`messages`/היסטוריה (עם `simulated=True` בשניהם -
`extract.update_lead_status` קיבל פרמטר `simulated` חדש שמעביר הלאה ל-
`_append_history`, מראה בדיוק את `log_manual_reply` הקיים), וליצור משימת מעקב -
**בדיוק כמו שליחה מוצלחת אמיתית**, כדי לאמת שכל שאר הצינור (שליפת ליד מה-CRM,
ניסוח הודעה עם Claude, עדכון סטטוס, יצירת משימה) מתפקד במלואו. שגיאה אמיתית
לא-קשורה-ל-Trial עדיין מדווחת כ`error` רגיל, בלי לשנות כלום ב-CRM (**לא**
"בולעת" כשל אמיתי). ה-CLI (`_print_campaign_results`) מציג שלוש תוצאות אפשריות
בבירור: `✅ נשלח בפועל` / `🧪 סימולציה (Trial)` / `❌ שליחה נכשלה`.

**נבדק בפועל:** `--send` על ליד בודד (עידו, המספר האישי של המשתמש, שכבר הצטרף
ל-Sandbox אך לא אומת כ-Verified Caller ID) - הודעה נדחתה ע"י Twilio בדיוק כצפוי,
נתפסה כ-`simulated`, ואומת מול ה-API/`customers.json`/`messages` ש-`lead_status`
עודכן ל-`contacted`, ההיסטוריה מכילה את ההודעה עם `simulated: true` ותחילית
ברורה ("סימולציה - חשבון Twilio Trial חסם..."), ושורת `messages` המתאימה עם
`simulated: True`. כל נתוני הבדיקה (כולל 3 משימות מעקב כפולות שהצטברו מניסיונות
קודמים באותה שיחה, לפני התיקון) נוקו ואומתו מול ה-API חזרה ל-11 לידים בסיס,
עידו ל-`lead_status: null`. **`/api/reactivate` (הדשבורד) נהנה מאותה התנהגות
אוטומטית** - זו אותה פונקציה בדיוק, בלי קוד נפרד.

### הצגת סוג נכס/תקציב בממשק (2026-08-30)

**מה נוסף:** `property_type`/`budget` (החולצים אוטומטית מהודעה קולית - ראו למטה)
נחשפו מ-`/api/leads` כבר קודם, אבל לא הוצגו בשום מקום ב-`index.html` - זה היה
פער ידוע. נוסף `propertyBadgeChips`/`propertyBadgesHtml` (חדש) - תגיות (badges)
בולטות וצבעוניות (🏠 סוג נכס - כחול `accent`, 💰 תקציב - כתום `status-serious`,
צבע שלא נוצל עדיין באף מקום אחר בממשק כדי לא להתנגש חזותית עם score-badge/status
הקיימים), **מוצגות רק כשיש ערך בפועל** (רוב הלידים שאינם נדל"ן לא רואים כלום -
בלי "ריקים" מכוערים). שני מיקומים, אותה לוגיקה בדיוק:
1. **בטבלה** - בתוך תא השם, מתחת לשורת "עסק · מיקום" הקיימת.
2. **בכרטיס הלקוח** (פאנל נגלל + Inbox) - `div` חדש (`panelPropertyWrap`/
   `inboxChatPropertyWrap`) מתחת לתג ציון החום, מתעדכן דרך `syncStatus()`
   הקיימת ב-`createChatController` (אותו מנגנון בדיוק שכבר מרענן את תג הציון) -
   **לא** נדרש קוד רענון נפרד. `:empty { display: none }` מונע רווח "רפאים"
   כשאין לליד את השדות (רוב הלידים).

**נבדק בפועל (Playwright, לא רק בקוד):** הוגדרו זמנית `property_type`/`budget`
על כרטיס בדיקה - שני הצבעים מוצגים נכון בטבלה **וגם** בפאנל עם 0 שגיאות
קונסולה; נבדק גם המקרה ההפוך (ליד בלי השדות) - אין רווח ריק/תג "ריק", הכפתורים
(שיחה/משימות) יושבים ישר אחרי תג הציון בדיוק כמו לפני השינוי. נתוני הבדיקה
שוחזרו ואומתו מול ה-API.

### חילוץ שדות אוטומטי מהודעה קולית עם gpt-4o-mini (2026-08-30)

**מה נוסף:** עד עכשיו הודעה קולית נכנסת רק תומללה (Whisper) והצטרפה לזרם הטקסט
הרגיל - חילוץ שדות (אם קרה בכלל) עבר רק דרך `extract_customer_info` הכללי
(Claude, לא ממוקד נדל"ן). המשתמש ביקש קריאה **נוספת ומפורשת**, מיד אחרי תמלול
מוצלח, ל-OpenAI **gpt-4o-mini** (לא Claude - נבחר במפורש) שמחלצת שלושה שדות
ממוקדי-נדל"ן: `customer_name`, `property_type` ("סוג נכס"), `budget` ("תקציב").

**קוד:** `transcription.extract_voice_message_fields(text)` (חדש) - קריאת
Chat Completions עם `model="gpt-4o-mini"`, `response_format={"type":"json_object"}`
(JSON תקין מובטח), `temperature=0` (דטרמיניסטי לחילוץ שדות, לא יצירתי).
`extract.update_lead_voice_extraction` (חדש) - כותב את השדות לכרטיס:
`property_type`/`budget` **תמיד** מתעדכנים לערך האחרון שנאמר (לא מצטברים),
אבל `customer_name` מתעדכן **רק אם עדיין לא ידוע בכרטיס** - כדי לא לדרוס שם
קיים בגלל חילוץ פחות אמין מתמלול קולי. מטא-דאטה בלבד, לא נוגע ב-history/
lead_status. מחובר ב-`/webhook`: רץ רק אחרי תמלול **מוצלח** (`voice_result[
"success"]`), על הטקסט הגולמי (בלי קידומת "🎙️"); כשל בשלב הזה נתפס בנפרד
(`try/except` צר) ולא מפיל את שאר הטיפול בהודעה - היא כבר תירשם כרגיל בהמשך
גם אם החילוץ נכשל. `property_type`/`budget` נחשפים גם דרך `GET /api/leads`.

**⚠️ תגלית חשובה תוך כדי בדיקה - שתי מערכות חילוץ עצמאיות פועלות על אותו
טקסט:** הטקסט המתומלל עובר **גם** דרך `update_lead_voice_extraction` (gpt-4o-mini,
עם ה"הגנה" על שם קיים) **וגם**, בהמשך אותה בקשת webhook, דרך `process_message_
with_reply` → `extract_customer_info` הרגיל (Claude, **בלי** הגנה כזו - תמיד
דורס `customer_name` אם משהו חולץ, התנהגות קיימת מלכתחילה, לא השתנתה כאן).
בבדיקה, טקסט שהזכיר שם שונה משם קיים בכרטיס ("קוראים לי יובל" על כרטיס בשם
"דני") - ה"הגנה" ב-`update_lead_voice_extraction` מנעה נכון את הדריסה שלה
עצמה, אבל אז ה-pipeline הקיים דרס בכל זאת (כי הוא באמת מצא "יובל" בטקסט).
**זו לא באג** - עקבי עם איך שהמערכת התנהגה תמיד לכל הודעת טקסט (גם לא-קולית)
שמזכירה שם שונה; ה"הגנה" ב-`update_lead_voice_extraction` עדיין שימושית באופן
עצמאי (למשל אם בעתיד ה-gpt-4o-mini extraction ישמש בלי ה-Claude pipeline
המלא), רק לא "המילה האחרונה" בזרימה הנוכחית של `/webhook`.

**נבדק בפועל (סבב ראשון, אותו יום):** קריאה אמיתית ל-Whisper וגם ל-gpt-4o-mini
עם `OPENAI_API_KEY` שהמשתמש עדכן (165 תווים ASCII, פורמט `sk-proj-` תקין) -
**שתיהן נכשלו עם 401**; נבדק ישירות מול תגובת השגיאה הגולמית של OpenAI (בלי
להדפיס את המפתח עצמו בשום שלב) - `"code": "invalid_api_key"`. **זו הייתה
הפעם השנייה** שמפתח שהוגדר ב-`.env` נמצא לא-תקין (הפעם הראשונה - מחרוזת
עברית במקום מפתח). מכיוון שהצלחה אמיתית הייתה חסומה רק ע"י המפתח, אומת אז
רק החיווט עם Flask `app.test_client()` + `unittest.mock.patch.object` (מדמה
את שתי הקריאות ל-OpenAI).

**✅ עדכון - סבב שני, אותו יום (2026-08-30): אומת בהצלחה מלאה עם קריאות
OpenAI אמיתיות מקצה-לקצה, אחרי שהמשתמש עדכן מפתח חדש.** נבנה סקריפט בדיקה
ייעודי (`test_ai_extraction_e2e.py`, בתיקיית scratchpad) שהולך צעד אחד רחוק
יותר מבדיקת החיווט הקודמת: (1) בדיקת תוקף מפתח זולה (`GET /v1/models`) לפני
שמבזבזים על קריאות יקרות יותר; (2) **יצירת אודיו עברי אמיתי** דרך OpenAI TTS
(`tts-1`, `voice=alloy`) ממשפט נדל"ן ריאליסטי - כדי ש-Whisper יתמלל דיבור
אמיתי, לא טון סינתטי כמו בבדיקה הקודמת; (3) שליחת `/webhook` (Flask test
client) עם האודיו הזה - **רק** `download_twilio_media` מדומה (אין URL מדיה
אמיתי מ-Twilio זמין כאן), אבל Whisper, gpt-4o-mini, וכתיבה ל-`customers.json`/
`messages` - כולם קריאות/כתיבות **אמיתיות**. **התוצאה: PASS מלא** - Whisper
תימלל את המשפט נכון (כמעט מדויק - "אורית לוי" יצא "אורית לי", סטייה קלה
טיפוסית ל-STT, לא שגיאת קוד), gpt-4o-mini חילץ `property_type: "בית פרטי"`
ו-`budget: "עד שלושה מיליון ומאתיים אלף שקל"` נכון, ההודעה נרשמה ב-`messages`
עם קידומת "🎙️", ו-Claude הגיב תגובה טבעית ומתייחסת לפרטים. **המסקנה: תשתית
התמלול+החילוץ האוטומטי עובדת במלואה בפועל, לא רק בתיאוריה** - זה סוגר את
הפער האחרון שתועד ב"חזון המוצר" סעיף ה' (מלבד פנייה אוטומטית על שדות חסרים,
שעדיין לא ממומשת). כל נתוני הבדיקה (כרטיס "דני"/0501234567, שורות `messages`)
נוקו ואומתו מול ה-API חזרה ל-11 לידים בסיס.

### אימות עומק לתמלול + ניהול תבניות/פרומפטים לסוכנים (2026-08-30)

**אימות חיבור OPENAI_API_KEY (לא רק "לא מוגדר"):** עד עכשיו רק המסלול "לא מוגדר"
נבדק בפועל (400 ידידותי). כדי לוודא שהקוד *באמת* יודע לקרוא ולהשתמש במפתח כשיהיה
אחד אמיתי - לא רק שהוא נכשל בעדינות בלעדיו - הוגדר זמנית `OPENAI_API_KEY` מזויף
ב-`.env` (עם גיבוי ושחזור מיידי אחרי הבדיקה, בדיוק כמו בדיקת "container טרי"
הקודמת). התוצאה: השרת קרא את המפתח נכון, `transcription.transcribe_audio` דילג
על בדיקת "לא מוגדר" ושלח בקשה אמיתית ל-`api.openai.com` - וקיבל **401 Unauthorized
אמיתי מ-OpenAI** (לא שגיאת "לא מוגדר" מקומית) - כלומר הבקשה עצמה (headers, endpoint,
multipart) הייתה תקינה מספיק שהשרת של OpenAI בכלל התייחס אליה, ורק האימות נכשל
(מפתח מזויף). זו ההוכחה הכי חזקה שאפשר בלי מפתח אמיתי שהאינטגרציה **תעבוד** ברגע
שיוגדר מפתח אמיתי. `.env` שוחזר מיד לאחר מכן.

**ניהול תבניות/פרומפטים לסוכנים:** `prompts.json` (חדש) - מקור אמת בזמן ריצה לכל
חמשת הפרומפטים שהיו קודם קבועים קשיחים בקוד (`EXTRACTION_PROMPT`/`REPLY_PROMPT`/
`CALL_SUMMARY_PROMPT` ב-`extract.py`, `OUTREACH_PROMPT`/`CLASSIFY_PROMPT` ב-
`reactivate.py`). `prompts.py` (חדש) - `get_prompt(key, default, vertical=None)`:
טוען מ-`prompts.json` אם קיים, אחרת נופל חזרה ל-default (הקבוע הקשיח שעדיין קיים
בקוד כרשת ביטחון - **לא הוסר**, רק הפך לברירת מחדל). כל פונקציית generate_* ב-
`extract.py`/`reactivate.py` שונתה לקרוא ל-`prompts.get_prompt(...)` במקום להשתמש
בקבוע ישירות.

**מותאם-ענף (vertical_overrides):** `reactivation_outreach` (סוכן ההחייאה) תומך
ב-override נפרד לכל ורטיקל (`ecommerce`/`services`/`real_estate`) - אם ריק לענף
מסוים, נופל חזרה לתבנית הבסיסית. `reactivate.generate_outreach_message` מקבל
`vertical` אופציונלי, ו-`run_reactivation_campaign` מעביר את `contact["vertical"]`
(מ-contacts.csv - כבר קיים, לא שדה חדש) אוטומטית. **הוספה נלווית**: `scheduler.py`'s
`VERTICAL_COLD_THRESHOLDS_DAYS` קיבל גם "services" (7 ימים - בין ecommerce ל-
real_estate) - משלים את שלושת הענפים ממסמך האפיון (חזון המוצר למעלה: נדל"ן/איקומרס/
שירותים) שעד עכשיו רק שניים מהם היו מיוצגים שם.

**UI:** כפתור "🤖 תבניות וסוכנים" בתפריט העליון פותח פאנל רחב (`.panel-wide`,
640px - התבניות ארוכות מדי לפאנל הרגיל) עם כרטיס לכל פרומפט (תווית + תיאור + textarea
+ שמור), ול-`reactivation_outreach` בלבד - גם שלוש תיבות override קטנות לכל ורטיקל.

**נבדק בפועל:** GET/POST על `/api/prompts` (כולל דחיית ורטיקל לא-תקין ו-key לא-קיים
עם 400/404 מתאימים), ו**אימות ש-Claude באמת מגיב שונה** - נשמרה תבנית בסיס עם
מילת-סימון "MARKERBASE" ו-override ל-`real_estate` עם "MARKERREALESTATE", ונקרא
`generate_outreach_message` ישירות עם `vertical="real_estate"`/`"ecommerce"`/
`None` - רק real_estate קיבל בחזרה את ה-override, השניים האחרים קיבלו את הבסיס.
`prompts.json` שוחזר לתוכן המקורי אחרי הבדיקה. Playwright: 5 כרטיסים, 3 תיבות
override, שמירה משתקפת אחרי סגירה+פתיחה מחדש של הפאנל, שמירה וניקוי override.
0 שגיאות קונסולה. **תוך כדי הבדיקות הנרחבות של כל הפיצ'רים האלה נמצא שריד נתוני-
בדיקה מתור החיוגים הרציף מה-turn הקודם** (שורות `calls` יתומות למיכל/רוני, כי
התור קורא ל-`/api/calls/start` על כל ליד שהוא עובר אליו - כולל דלוגים - לא רק
מי שסיים הערות; ניקוי קודם כיסה רק את הליד שבאמת השלים שיחה) - גרם לניפוח מלאכותי
של ציון החום שלהם (45 במקום 1). נוקה ואומת מול ה-API חזרה ל-11 לידים בסיס.

### חיוג רציף (Dial Queue) + תשתית תמלול קולי (STT) (2026-08-30)

**סוגר שני פערים שתועדו מפורשות ב"חזון המוצר" למעלה** אחרי שהמשתמש ביקש לבנות
אותם בפועל.

**חיוג רציף:** לא endpoint חדש - הרחבה טהורה של פאנל ה-Click-to-Call הקיים
(`#callPanel`) ב-`index.html`. `dialQueue` (state: `{items:[{phone,tenantId,
name}], index}`) נבנה מ-`selectedLeads` (אותו Set שכבר משמש את "עדכן סטטוס
לנבחרים") בלחיצה על "📞 התחל חיוג רציף" בפס הפעולות (`#bulkBar`). כל חמשת
מצבי הפאנל (`renderCallPanelCalling`/`InProgress`/`NotesForm`/`Summarizing`/
`Done`) עוברים עכשיו דרך `renderCallBody()` משותפת שמוסיפה תמיד: (1) סרגל תור
("ליד X מתוך Y" + "⏭ דלג" + "⏹ עצור תור") כשהתור פעיל, (2) קישורי `tel:`/
`wa.me` מהירים - **תמיד** זמינים, לא תלויים בתצורת Twilio Voice (שעדיין לא
מוגדרת) - נבנים עם `toE164ish()` (מראה `_to_e164` הפייתוני בצד הלקוח). אחרי
תקציר שיחה (`renderCallDone`), אם התור פעיל מוצג כפתור "✅ סיום שיחה - לליד
הבא בתור" שקורא ל-`advanceDialQueue()` - פותח את `openCallPanel` הבא ישירות,
בלי לחזור לטבלה. "⏭ דלג" זמין בכל שלב (למשל ליד שלא עונה) - לא שומר הערות,
רק מתקדם. סגירת הפאנל (❌) מבטלת גם תור פעיל - לא ממשיך "ברקע".

**🐛 באג שנמצא ותוקן תוך כדי בדיקה:** קישור `wa.me` נבנה במקור ישירות מ-
`activeCall.phone` בלי נרמול - ליד בפורמט מקומי ("0501234567") הפיק קישור
`wa.me/0501234567` **לא תקין** (וואטסאפ דורש קידומת מדינה מלאה, בלי 0 מוביל).
תוקן עם `toE164ish()` - נבדק: `tel:+972501234567`/`wa.me/972501234567` נכונים.

**תמלול קולי (STT):** `transcription.py` (חדש) - עטיפת HTTP ישירה (`requests`,
בלי תלות pip חדשה - כבר טרנזיטיבי דרך twilio/anthropic, ועכשיו גם מוצהר
במפורש ב-`requirements.txt`) ל-OpenAI Whisper API. `POST /api/calls/<id>/
transcribe` מקבל קובץ אודיו, מחזיר טקסט - **לא שומר כלום בעצמו**. ה-UI ממלא
את הטקסט המתומלל לתוך אותה תיבת הערות שמזינה את `/api/calls/<id>/notes`
הקיים - כך שכל צינור השמירה (תקציר AI → כרטיס + חותמת זמן + היסטוריה) זהה
לגמרי בין הקלדה ידנית לתמלול, בלי קוד כפול. אם `OPENAI_API_KEY` לא מוגדר -
מחזיר 400 עם הודעה ברורה, וה-UI מציג אותה ומשאיר את תיבת ההערות זמינה
להקלדה ידנית (אותו דפוס בדיוק כמו כל "לא מוגדר" אחר בפרויקט - Voice/משוב).

**נבדק בפועל:** תור מלא על 3 לידים (API + Playwright) - "דלג" והתקדמות דרך
"סיום שיחה" שניהם מתקדמים נכון (1→2→3→"סיימת את התור"), קישורי חיוג/וואטסאפ
מוצגים ומנורמלים נכון, `/api/calls/<id>/transcribe` מחזיר 400 ברור כשלא
מוגדר וגם 404 על call_id לא קיים. **לא נבדק (ולא ניתן כאן):** תמלול אמיתי
מול Whisper API - דורש `OPENAI_API_KEY` שהמשתמש טרם סיפק. 0 שגיאות קונסולה.
כל נתוני הבדיקה נוקו.

### החייאת לידים קרים לפי ימים + ייצוא/ייבוא CSV מלא (2026-08-30)

**החייאה לפי ימים (לא רק סטטוס):** `reactivate.get_cold_leads` תמך עד עכשיו רק בסינון
סטטוס (contacted/hot/not_relevant מוציאים ליד מ"קר") - **לא** בדק זמן בכלל. בדיקת-זמן
פר-ורטיקל כבר הייתה קיימת, אבל רק ב-`scheduler.py` (המנוע האוטומטי ברקע) - לא בכפתור
"🔥 החייאת לידים קרים" הידני בדשבורד. נוסף פרמטר `days` אופציונלי (ברירת מחדל **None**
- לא DEFAULT_COLD_DAYS! - כדי לא לשנות בטעות את התנהגות `scheduler.py` הקיימת, שמסתמכת
על הסף המהיר של ecommerce, 3 ימים; סף כללי של 30 יום כברירת מחדל היה "בולע" את הסף
המהיר הזה). `/api/reactivate` מעביר במפורש `days=30` (או ערך מה-UI - שדה קלט חדש
"לא נוצר קשר מעל __ ימים" בפאנל). `last_contact_at` (בדיקת זמן הפעילות האחרונה
מהיסטוריית הכרטיס) חולצה מ-`scheduler.py` ל-`extract.py` כפונקציה משותפת - נמנעת
כפילות קוד, `scheduler.py` עצמו רק קורא לה עכשיו.

**פעילות מעקב אוטומטית:** כל הרצת `send=True` (גם אם השליחה בפועל מול Twilio נכשלה,
למשל Trial) יוצרת עכשיו משימת `calendar_tasks` ("מעקב אחרי חימום: <שם>", בעוד 3 ימים)
לכל ליד - כדי שלנציג יהיה תמיד action item למעקב, גם אם הערוץ הטכני נכשל. **לא**
נוצרת משימה בתצוגה מקדימה (send=False) - רק בפעולת שליחה מפורשת, כדי לא ליצור
כפילויות בכל לחיצה חוזרת על "תצוגה מקדימה".

**ייצוא CSV:** `GET /api/leads/export` - כל הלידים + ציון חום מחושב, BOM ל-Excel.
העמודות תואמות בכוונה למה שנתמך גם בייבוא (חוץ מ-score, שדה נגזר בלבד).

**🐛 באג אמיתי שנמצא ותוקן: ייבוא CSV יצר לידים כפולים (לא רק "לא דרס" - ממש
שכפל).** `/api/leads/import`/`POST /api/leads` נרמלו טלפון ל-E.164 (`_to_e164`)
*לפני* בדיקת אם הליד כבר קיים - אם כרטיס כבר שמור בפורמט מקומי (למשל "0501234567",
בדיוק המגבלה הידועה שכבר מתועדת ב-"מגבלות ידועות" למעלה), הנרמול יצר מפתח **שונה**
("+972501234567") וייבוא-חוזר של קובץ שיוצא מ-`/api/leads/export` יצר כרטיס כפול
במקום לעדכן את הקיים - **בדיוק מה שהמשתמש חשש ממנו** ("בלי לדרוס או לשבור את
המבנה"). **גרוע מזה: תוך כדי בדיקת round-trip, זה גם דרס בטעות כרטיס אחר קיים**
(רוני/רוני קייטרינג, שכבר היה שמור תחת אותו E.164 שדני מתנרמל אליו - התנגשות עם
הכפילות הידועה מ-2026-08-25) - שוחזר ידנית מנתוני backup מוקדמים מהשיחה. **תוקן**:
`extract.resolve_existing_phone(phone, tenant_id)` בודק גם את הפורמט הגולמי וגם
E.164 מול `customers.json` *לפני* שמחליטים על מפתח - אם כרטיס קיים בכל פורמט,
ממשיכים לעדכן אותו; רק אם אין התאמה בשום פורמט, נוצר כרטיס חדש בפורמט המנורמל.
משמש גם ב-`/api/leads/import` וגם ב-`POST /api/leads`. **לא** ממזג/מתקן כרטיסים
כפולים שכבר קיימים מהעבר (עדיין דורש ניקוי ידני כמתועד) - רק מונע כפילות *חדשה*.
**נבדק בפועל**: round-trip מלא (ייצוא כל הלידים → ייבוא חזרה, פר-tenant כמו שהתכנון
המקורי מצפה) - `imported=0, updated=11`, אפס מפתחות חדשים/נעלמים, כרטיס דני זהה
בייט-לביט לפני/אחרי. גם נבדק שהוספת ליד חדש בפורמט מקומי ואז שוב באותו מספר בפורמט
E.164 מתעדכנים על אותו כרטיס (לא כפילות), וגם שליד *באמת* חדש עדיין נוצר כרגיל.

### ציון חום לליד (Lead Scoring) + חיפוש/סינון מתקדם (2026-08-30)

**ציון חום:** `db.compute_lead_score(phone, tenant_id)` - חישוב **נגזר** (derived), לא
נשמר בשום מקום - בדיוק כמו `get_last_message`, נמנעים מבעיית invalidation בכל פעם
שמתווספת פעילות. רכיבי הציון (סה"כ מקסימלי מובטח = 100, רצפה = 1): הודעות וואטסאפ
(עד 35 נק', 4 נק'/הודעה), שיחות (עד 25 נק', 10 נק'/שיחה), משימות שהושלמו (עד 15 נק',
5 נק'/משימה), עדכניות הפעילות האחרונה מכל אחד מהשלושה (עד 25 נק', יורד ככל שעבר יותר
זמן). נחשף דרך `GET /api/leads?include_score=1` (opt-in - אותו דפוס בדיוק כמו
`include_last_message`, כדי לא להכביד על טעינת הטבלה כשלא נדרש) - אבל בפועל
`loadLeads()` ב-`index.html` **תמיד** מבקש אותו כי הציון מוצג תמיד בטבלה + בכרטיס
הלקוח (`panelScoreWrap`/`inboxChatScoreWrap`, מתעדכן דרך `createChatController`'s
`syncStatus()`). **צבעים (לפי דרישת המשתמש, הפוך מהאינטואיציה של תג הסטטוס
"חם"=ירוק הקיים):** אדום (`score-badge.hot`, ≥70) לחם, כתום (`warm`, 40-69) לפושר,
כחול (`cold`, <40) לקר - במכוון *לא* ירוק, כדי לא להתנגש חזותית עם `.badge.hot`
הקיים של lead_status (מושג שונה לגמרי - סטטוס ידני מול ציון אוטומטי-מחושב).

**קטגוריה חופשית:** שדה טקסט חדש על כרטיס הלקוח (`category` - לא enum קבוע כמו
`lead_status`, כי הדוגמאות שניתנו - "נדל"ן"/"פרטי"/"משפחה" - הן open-ended).
`extract.update_lead_category` (מראה `log_manual_reply` - לא נוגע ב-history, מטא-דאטה
בלבד) + `POST /api/leads/category`. גם נתמך בייבוא CSV/Excel (`IMPORT_FIELD_ALIASES`).
עריכה inline בטבלה (`input.category-input`, נשמר ב-blur/Enter). מסנן הקטגוריות
(`#categoryFilter`) נבנה דינמית מהערכים שבפועל קיימים ב-`allLeads` - לא רשימה קבועה
מראש.

**חיפוש רב-מימדי:** שני מקורות ממוזגים (union, לא AND): (1) התאמה מקומית מיידית
בשדות שכבר בזיכרון (שם/טלפון/עסק/מיקום/קטגוריה) - בדיוק כמו החיפוש הישן, ללא שינוי
בזמן תגובה; (2) `GET /api/search?q=` (debounce 300ms) - מחפש גם בתוכן פעילות: טקסט
הודעות (`messages.message`), הערות/תקציר שיחה (`calls.notes`/`summary`), כותרת/הערות
משימות (`calendar_tasks.title`/`notes`) דרך `db.search_activity`, **וגם** בשדות
הכרטיס עצמו ב-`customers.json` (לא רק ב-`allLeads` הטעון בדפדפן - חשוב כי `/api/leads`
הרגיל לא כולל את כל השדות). התוצאה (`searchMatchSet`, Set של `tenant::phone`) ממוזגת
כ-OR עם ההתאמה המקומית ב-`getFilteredLeads`. **חיפוש substring רגיל (`LIKE '%q%'`),
לא ניתוח מילים** - זה מכוון (לא התבקש חיפוש טקסט מלא/fuzzy), אבל המשמעות היא שמונח
קצר עלול להתאים כתת-מחרוזת של מילה לא קשורה (למשל "פרטי" תואם גם את "ה**פרטי**ם") -
זו התנהגות צפויה, לא באג.

**נבדק בפועל (API ישיר + Playwright):** ציון עולה נכון אחרי הוספת פעילות (הודעה),
ציון יורד לרצפה (1) כשאין פעילות בכלל; חיפוש עמוק מוצא ליד לפי תוכן הודעה אמיתית;
סינון לפי טווח ציון/קטגוריה בטבלה; קטגוריה נשמרת ומופיעה מיד במסנן הדינמי; תג הציון
מופיע גם בטבלה וגם בכרטיס הלקוח (פאנל וגם Inbox). **תוך כדי בדיקה נמצאו שיירי נתוני
בדיקה מפיצ'ר Click-to-Call קודם** (שורת `calls` יתומה ל"דני" שלא נמחקה בניקוי ההוא) -
זו לא הייתה שגיאה בקוד הנוכחי, אבל נוקתה כעת; מזכיר שניקוי נתוני בדיקה צריך תמיד
להיבדק בפועל דרך ה-API אחרי המחיקה, לא רק להניח שסקריפט הניקוי הספיק. 0 שגיאות
קונסולה. כל נתוני הבדיקה של הפיצ'ר הזה (הודעה, קטגוריות) נוקו בסוף.

### משימות מעקב (Follow-ups) + משוב מערכת (2026-08-30)

**מקור:** המשתמש הדביק סקריפט `init_db()` עם ארבע טבלאות (`leads`, `interactions`,
`calendar_tasks`, `system_feedback`) כנקודת פתיחה. הסכמה הזו **סותרת** את הארכיטקטורה
הקיימת - מקור האמת ללידים הוא `customers.json` (דרך `extract.py`), לא טבלת `leads`
ב-SQL, ו-`crm_data.db` הוא תמיד לוג טכני משני (`messages`/`scheduler_runs`/`calls`)
לפי `phone`+`tenant_id`, לא לפי מזהה מספרי. **הוחלט (עם המשתמש) לבנות את שתי היכולות
שהסקריפט רומז אליהן, מותאמות לארכיטקטורה הקיימת - לא את הסקריפט כפי שהוא.**
`leads`/`interactions` נפלו לגמרי - `customers.json`/`messages` כבר מכסים את זה.

**משימות מעקב:** טבלת `calendar_tasks` חדשה ב-`db.py`, ממופתחת לפי `phone`+`tenant_id`
(כמו כל טבלה אחרת בקובץ) - **אין מזהה ליד מספרי בפרויקט הזה בכלל**. `db.get_tasks`
ממוינת לפי `due_date`/`due_time` עולה (הקרוב ביותר קודם) - בכוונה שונה מכל שאר
הטבלאות (שממוינות לפי זמן יצירה), כי מה שחשוב כאן הוא מתי המשימה אמורה לקרות. כפתור
"📅 משימות" (בפאנל הנגלל וב-Inbox, ליד "📞 שיחה") פותח פאנל פר-ליד עם רשימת המשימות +
טופס הוספה מהיר. כפתור "📅 יומן" בתפריט העליון פותח תצוגה **גלובלית** (כל הלידים,
מסוננת לפי `tenantFilter` הנוכחי) של משימות `pending` בלבד - קליק על שורה פותח את
הצ'אט של הליד (`openHistory`, בלי תצוגת-פרטים כפולה).

**משוב מערכת:** טבלת `system_feedback` חדשה - **לא קשורה ללידים בכלל**, פנימית לצוות
שמפעיל את ה-CRM (באג/רעיון על הכלי עצמו). כפתור "💡 משוב" בתפריט העליון. ה-`<select>`
של סטטוס המשוב מקבל class נפרד (`feedback-status-select`) מ-`.status-select` של
סטטוס לידים - כדי שלא יתנגש עם ה-listener הגלובלי הקיים שמאזין ל-`.status-select`
ושולח ל-`/api/leads/status`.

**רענון קטן תוך כדי:** הלוגיקה שנוספה ב-Click-to-Call לבדוק אם overlay צריך להישאר
פתוח כשסוגרים פאנל-משנה (כי `#panel` עוד open מתחתיו) חולצה מ-`closeCallPanel`
לפונקציה משותפת `closeSubPanel(id)` - כי עכשיו יש עוד שלושה פאנלים (`tasksPanel`/
`calendarPanel`/`feedbackPanel`) שצריכים בדיוק אותה בדיקה.

**נבדק בפועל (API ישיר + Playwright):** יצירת/עדכון/שליפת משימות (כולל אימות מיון
נכון לפי תאריך יעד), סימון "בוצע"/"בטל" מהפאנל הפר-ליד וגם מהיומן הגלובלי, קליק-דרך
מהיומן לצ'אט הליד, הוספת משוב (באג + רעיון) ושינוי סטטוס דרך ה-dropdown. **נמצא ותוקן
תוך כדי בדיקה**: ה-handler של לחיצות ביומן הגלובלי טיפל בכפתור "✓ בוצע" אבל לא ב-"✗
בטל" - קליק על "בטל" היה נופל ל-branch שפותח בטעות את הצ'אט של הליד במקום לבטל את
המשימה; תוקן להוסיף בדיקה מפורשת לכפתור הביטול לפני ה-fallback. 0 שגיאות קונסולה,
רגרסיה מלאה על שאר הדשבורד. כל נתוני הבדיקה נוקו (`calendar_tasks`/`system_feedback`).

### Click-to-Call + תקציר שיחה אוטומטי (2026-08-27)

**מנגנון:** כפתור "📞 שיחה" (בפאנל ההיסטוריה הנגלל וב-Inbox, ליד ה-header של הצ'אט)
פותח פאנל ייעודי (`#callPanel`) וקורא ל-`POST /api/calls/start`. זה יוזם **גישור** דרך
Twilio (`voice_call.start_bridge_call` - חדש): Twilio מתקשר קודם לנציג
(`AGENT_PHONE_NUMBER`), וברגע שהוא עונה, TwiML ב-`/voice/connect` מגשר (`<Dial
record="record-from-answer">`) למספר הלקוח - **לא** Softphone מבוסס-WebRTC בדפדפן
(זה היה דורש יצירת TwiML Application ידנית בקונסולת Twilio, לא ניתן לסקריפט + גישה
למיקרופון). ההקלטה נשמרת (URL בלבד, דרך `/voice/recording-status`) - **אין תמלול/STT
אוטומטי** בגרסה הזו (הוחלט במפורש מול המשתמש: "הקלטה + הערות ידניות" עדיף על תמלול
Twilio המובנה - איכות מוטלת בספק בעברית - ועל הוספת ספק STT חדש כמו OpenAI Whisper -
עלות/תלות חדשה). אחרי השיחה, הנציג מקליד הערות חופשיות (`POST
/api/calls/<id>/notes`), ו-Claude מייצר מהן תקציר עברי קצר
(`extract.generate_call_summary` - אותו pattern בדיוק כמו `generate_reply`/
`generate_outreach_message`). התקציר נשמר בטבלת `calls` החדשה ב-`db.py` (audit trail:
`call_sid`/`status`/`duration_seconds`/`recording_url`/`notes`/`summary`) **וגם**
משוקף להיסטוריה הרגילה (`extract.log_call_summary`, בכוונה לא נוגע ב-`lead_status` -
כמו `log_manual_reply`) **וגם** לטבלת `messages` עם `channel="voice"` - כך שהוא מופיע
אוטומטית כבועה בצ'אט הקיים (פאנל/Inbox) דרך אותו polling שכבר קיים ונבדק, בלי קוד
רינדור נוסף (`renderBubble` קיבל רק הסתעפות קטנה ל-`channel==="voice"`).

**Fallback מדומה - בדיוק כמו Trial ב-WhatsApp:** בסביבת הפיתוח הזו **אין** עדיין מספר
Twilio Voice/`AGENT_PHONE_NUMBER`/`PUBLIC_BASE_URL` מוגדרים ב-`.env`. `/api/calls/start`
תופס את זה (`RuntimeError` מ-`voice_call.py`, נבדק כ-`status="simulated_no_config"`)
ומתנהג בדיוק כמו `_is_trial_restriction` ב-`/api/messages/send`: רושם שיחה כ-`simulated`
ומחזיר 200 (לא 502) - כדי לאפשר לבדוק את כל שאר הצינור (הערות → Claude → תקציר → כרטיס)
בלי מספר Voice אמיתי.

**נבדק בפועל (Playwright + קריאות API ישירות), לא רק בקוד:**
- `POST /api/calls/start` → `simulated:true, status:"simulated_no_config"` כצפוי.
- `POST /api/calls/<id>/notes` עם הערות עבריות אמיתיות → תקציר Claude איכותי ותמציתי
  (לא stub - `ANTHROPIC_API_KEY` כבר מוגדר), נשמר גם ב-`calls` וגם ב-`customers.json`
  history (`channel:"voice"`) וגם ב-`messages`; אומת ש-`lead_status` **לא** השתנה.
- Playwright: כפתור "📞 שיחה" מהפאנל הנגלל וגם מה-Inbox - שניהם פותחים את `#callPanel`
  נכון (כולל stacking נכון מעל הפאנל/ה-Inbox, ו-overlay שנשאר פתוח אם `#panel` עדיין
  פתוח מתחתיו). התקציר שנוצר הופיע כבועה חדשה עם תווית "📞 תקציר שיחה" בצ'אט תוך מחזור
  polling אחד אחרי סגירת פאנל השיחה - **בלי רענון ידני**, בדיוק כמו שיחת webhook נכנסת.
  0 שגיאות קונסולה, ורגרסיה מלאה: שליחת הודעה רגילה דרך הקומפוזר הקיים ב-Inbox עדיין
  עובדת אחרי כל השינויים.
- **לא נבדק (ולא ניתן לבדיקה כאן):** חיוג אמיתי לטלפון נציג, `/voice/connect` שנקרא
  בפועל ע"י Twilio ומגשר ללקוח, הקלטה אמיתית + `/voice/recording-status` עם
  `RecordingUrl` אמיתי, מעברי `CallStatus` אמיתיים ב-`/voice/status`, ואימות חתימת
  Twilio אמיתי (`_verify_twilio_request`) על שלושת ה-webhooks החדשים - דורש מספר Voice
  אמיתי + `AGENT_PHONE_NUMBER` + `PUBLIC_BASE_URL` ציבורי (ngrok/Render) שהמשתמש טרם
  סיפק. כל נתוני הבדיקה נוקו בסוף (`calls`, `messages`, `customers.json` history).

### פריסה לענן (2026-08-26) - Procfile, Gunicorn, נתיבים, ומגבלה קריטית

**מה נוסף ונבדק:**
- **`Procfile`** - שני process types (מוסכמת Heroku/Render/Railway):
  `web: gunicorn server:app --bind 0.0.0.0:$PORT --workers 2 --timeout 60` ו-
  `clock: python scheduler_worker.py`. **הפרדה מכוונת, לא שרירותית:** `scheduler.start()`
  (thread בתוך `server.py`) עובד רק בהרצה ישירה (`python server.py`, `__name__ ==
  "__main__"`) - Gunicorn לא מפעיל את הבלוק הזה בכלל. גם אם היה מופעל, כל אחד מה-
  workers של Gunicorn היה מריץ עותק נפרד של הלולאה - ואם `SCHEDULER_AUTO_SEND=true`
  אי פעם, זו הייתה שליחה כפולה/משולשת של אותה הודעה. `scheduler_worker.py` (חדש, עם
  APScheduler - נוסף ל-`requirements.txt`) הוא process יחיד ונפרד ל-production, בלי
  הבעיה הזו. **נבדק ישירות**: `python scheduler_worker.py` בפועל - עלה, הריץ סריקה
  מיידית (זהה להתנהגות ה-thread הקודמת), רשם ל-`chat_history.txt`, נעצר נקי.
  ⚠️ **לא הצלחתי להריץ את Gunicorn עצמו** במכונת הפיתוח הזו - Gunicorn תלוי ב-`fcntl`
  (מודול POSIX-בלבד) ו**לא רץ על Windows בכלל** מבחינה עקרונית, לא רק כאן. זה לא
  משפיע על הפריסה בפועל (יעד הפריסה הוא קונטיינר Linux), אבל המשמעות היא של-Gunicorn
  עצמו (בניגוד לכל שאר הקוד בפרויקט) **לא בוצעה הרצה אמיתית מקצה-לקצה** בסביבה הזו -
  רק ייבוא `server:app` כאובייקט WSGI תקין אומת (ראו למטה).
- **`requirements.txt` מלא:** `anthropic`, `python-dotenv`, `flask`, `twilio`, `openpyxl`,
  `gunicorn`, `apscheduler` - כל התלויות הישירות שבפועל בשימוש בקוד.
- **נתיבים יחסיים - נבדק בפועל, לא רק בקוד:** כל קובץ (`customers.json`, `crm_data.db`,
  `contacts.csv`, `chat_history.txt`, `server_error.log`, `.env`) נטען דרך
  `Path(__file__).parent / "..."` - לא תלוי בתיקיית העבודה (cwd) שממנה מריצים את
  התהליך. **תוך כדי בדיקה נתקלתי בבאג אמיתי וצר**: `load_dotenv()` בלי נתיב מפורש
  משתמש בזיהוי stack-frame שנכשל דווקא כש-`python -c "..."` (הרצת קוד inline) מייבא
  את `server`; כשמייבאים כקובץ אמיתי (כך ש-Gunicorn/`import` רגיל עובדים) זה כן הצליח
  - אבל כדי לא להסתמך על ההתנהגות העדינה הזו בכלל, שיניתי את **כל** קריאות ה-
  `load_dotenv()` (ב-`server.py`, `extract.py`, `reactivate.py`, `whatsapp_send.py`,
  `scheduler.py`) לנתיב מפורש (`dotenv_path=Path(__file__).parent / ".env"`) - וגם
  את `create_db.py` (שהשתמש בנתיב יחסי גולמי `'crm_data.db'`) לאותה גישה. נבדק: ייבוא
  `server:app` מ-cwd שונה לגמרי, וגם קריאה אמיתית מ-`crm_data.db` דרך אותו ייבוא -
  שניהם הצליחו ומצאו את הנתונים האמיתיים, לא קבצים ריקים חדשים.

**✅ עודכן 2026-08-26 - מגבלת ה-ephemeral filesystem טופלה חלקית:** `customers.json`,
`crm_data.db` ו-`chat_history.txt` (State שמשתנה בזמן ריצה) עוברים עכשיו דרך `paths.py`
חדש - `DATA_DIR` (env var, ברירת מחדל: תיקיית הפרויקט - **אין שינוי בהתנהגות מקומית**).
ב-Render, מגדירים Disk עם Mount Path (למשל `/data`) ו-`DATA_DIR=/data` באותו ערך -
כך הנתונים שורדים בין דיפלויים. `contacts.csv` (seed/קלט, לא state) ו-`server_error.log`
(לוג טכני, לא קריטי לשימור) **נשארים** בתיקיית הפרויקט במכוון - ראו הערת הראש של
`paths.py`. **נבדק בפועל**, לא רק בקוד: (1) ברירת מחדל בלי `DATA_DIR` - שלושת הנתיבים
זהים ל-100% למה שהיו לפני השינוי (בדיקת equality ישירה); (2) עם `DATA_DIR` מוצבע
לתיקייה נפרדת - קובץ שנוצר דרך `extract.upsert_customer` נחת שם ולא ב-`customers.json`
האמיתי של הפרויקט (וידאתי את שניהם); (3) הרצה מלאה מחדש של השרת + הבדיקה הפונקציונלית
המלאה (webhook חתום + 7 בדיקות נוספות) - עברה 100%, אפס שגיאות בלוג.
**עדיין לא מומש:** מעבר ל-DB מנוהל אמיתי (Postgres וכו') - `DATA_DIR` פותר את בעיית
ה-ephemeral filesystem, אבל `crm_data.db` נשאר SQLite קובץ-יחיד (לא תומך בכתיבה
מקבילית אמיתית בעומס, גם עם דיסק מתמיד) - זה עדיין מחוץ לסקופ שהתבקש.

**🐛 באג אמיתי שנמצא ותוקן 2026-08-26 - Render deploy נכשל עם "Exit status 1":**
ב-`server.py`, `import db` / `import reactivate` / `import scheduler` רצו **לפני**
בלוק ה-`try/except KeyError` שנועד לתפוס משתנה סביבה חסר (`ANTHROPIC_API_KEY`) בצורה
ברורה - אבל שלושתם תלויים ב-`extract.py` בעצמם, אז ה-`KeyError` קרה כבר ב-`import
reactivate` עצמו, **לפני** שהגיעו בכלל לבלוק שאמור לתפוס אותו. בפועל זה הופיע כ-
traceback גולמי לא ברור בלוג, ולא כהודעת השגיאה הידידותית שהתכוונתי אליה. **תוקן**
ע"י העברת שלושת ה-imports האלה *לתוך* אותו try/except. **נבדק בפועל**: הסתרתי זמנית
את `.env` (מדמה container טרי ב-Render בלי משתני סביבה מוגדרים עדיין) - לפני התיקון
קיבלתי traceback גולמי; אחרי התיקון: `שגיאת הגדרה: משתנה סביבה חסר - 'ANTHROPIC_API_KEY'.
בדקו את משתני הסביבה (.env מקומית / Render Environment)` עם `exit code 1` נקי. שחזרתי
את `.env` מיד אחרי כל בדיקה. **זה ה-hypothesis המוביל להסבר "Deploy failed error 1"
שדווח** - חסרים משתני סביבה בדשבורד של Render (ראו CLAUDE.md/שיחה קודמת לרשימת
המשתנים הנדרשים) - אבל לא אושר מול הלוג האמיתי של Render עצמו, רק שוחזר מקומית.

**⚠️ דיווח כשל דיפלוי נוסף ב-2026-08-30 (אחרי קומיטים Lead Scoring/חיפוש
מתקדם) - לא אותר קוד שבור, requirements.txt נעול כאמצעי זהירות:** המשתמש דיווח
שהדיפלוי ב-Render נכשל בעקבות אחד משני הקומיטים האחרונים (Lead Scoring/חיפוש
מתקדם, או שדרוג העיצוב שבא אחריו). **לא הייתה גישה ללוג האמיתי של Render** -
האבחון כולו מבוסס על שחזור מקומי. בוצע: (1) `py_compile` על כל קובצי ה-Python -
עבר נקי; (2) `from server import app` - בדיוק מה ש-Gunicorn עושה כדי לקבל את
האובייקט WSGI - עבר נקי, גם מ-cwd שונה; (3) **שחזור מדויק של אותה בדיקת "container
טרי" שתפסה את הבאג האמיתי הקודם** (הסתרת `.env` זמנית) - עדיין מניב את הודעת
השגיאה הידידותית והנכונה (`exit code 1` נקי), לא traceback גולמי - כלומר התיקון
הקודם עדיין תקף ולא נסוג; (4) הרצת שרת מלאה + smoke test על 13 נתיבים שנוספו/
השתנו בשלושת הקומיטים האחרונים (`/api/leads` על כל הצירופים, `/api/search`,
`/api/tasks`, `/api/feedback`, `/api/calls`, `/api/scheduler/*`) - כולם 200
OK; (5) `server_error.log` מהיום (2026-08-30) - **ריק לגמרי מ-WARNING/ERROR**,
כולל כל הבדיקות הנרחבות שבוצעו. **מסקנה:** לא נמצא קוד שבור בפועל בסביבה מקומית -
כולל בדיוק אותה שיטת בדיקה שתפסה את הבאג הקודם. הסבר הכי סביר שנותר, ולא ניתן
לאמת בלי גישה ללוג: **`requirements.txt` היה ללא נעילת גרסאות בכלל** - `pip
install -r requirements.txt` על build טרי ב-Render מושך את הגרסה העדכנית ביותר
של כל חבילה בזמן ה-build, שיכולה להיות **שונה** מהגרסאות שנבדקו כאן מקומית (ולא
להתאים זו לזו) - זה בדיוק סוג הפער שגורם ל"עובד אצלי, נכשל ב-build טרי". **תוקן**:
`requirements.txt` ננעל לגרסאות המדויקות שהותקנו ונבדקו בפועל בסביבה המקומית הזו
(`pip freeze`), ו-`pip install -r requirements.txt` הורץ מחדש מול הקובץ הנעול -
כולם "already satisfied", ללא התנגשויות תלות. **זו ההשערה המובילה, לא אישור
סופי** - אם הדיפלוי עדיין נכשל אחרי זה, נדרש טקסט הלוג האמיתי של Render כדי
להמשיך לאבחן (בדיוק כמו שקרה בבאג הקודם).

## עקרונות משותפים לכל הסוכנים

- **מקור אמת יחיד:** `customers.json`. כל הסוכנים קוראים/כותבים דרך הפונקציות ב-`extract.py`
  (`load_customers`, `upsert_customer`, `update_lead_status`, `import_lead`) — לא נוגעים
  בקובץ ישירות. אין להוסיף שדות/לוגיקה שכופלים או עוקפים את הפונקציות הקיימות.
  **לוג טכני נוסף (לא מקור אמת):** כל הודעה נכנסת/יוצאת נרשמת גם בטבלת `messages` ב-
  `crm_data.db` (SQLite) דרך `db.py` (`log_message`), וגם כשורת טקסט ב-`chat_history.txt`
  (דרך `_log_incoming_message` ב-`server.py`). אלו כפילויות מכוונות למעקב/דיבוג בלבד —
  אף לוגיקת עסקים לא קוראת מהן; מקור האמת היחיד להחלטות עדיין `customers.json`.
- **היסטוריה:** כל אינטראקציה עם לקוח (הודעה נכנסת, הודעת חימום שנשלחה) נשמרת במערך
  `history` בכרטיס הלקוח, עם `timestamp`, `channel`, `message`.
- **ערוץ (`source_channel`):** כל כרטיס לקוח נושא את הערוץ שממנו הגיע לראשונה. ערכים תקפים
  כרגע: `whatsapp` (אמיתי), `instagram`, `facebook` (placeholder בלבד — ראו הערה ב-`server.py`).
- **בטיחות:** אף סוכן לא סוגר עסקה, לא מוחק נתונים ולא יוזם פנייה ראשונה ללקוח בלי אישור
  אנושי מפורש. שליחת הודעה יזומה ראשונה בוואטסאפ (כמו חימום לידים קרים ב-`reactivate.py`)
  דורשת הודעת-תבנית מאושרת מול מטא — זה תהליך עסקי נפרד מהקוד, ועדיין לא ממומש.
  **חריגים (שליחה אוטומטית מותרת):**
  1. `server.py` שולח אוטומטית תגובת-מענה חזרה ללקוח בוואטסאפ (דרך TwiML) כשמתקבלת הודעה
     נכנסת — זו תגובה בתוך חלון השיחה שהלקוח פתח, לא פנייה יזומה, ולכן מותרת ללא תבנית.
  2. `POST /api/messages/send` (משמש את כפתור "שלח" בפאנל ההיסטוריה ב-`index.html`) שולח
     הודעה חופשית אמיתית דרך Twilio. האישור האנושי כאן הוא הקליק המפורש של הנציג על
     "שלח" לכל הודעה בנפרד (בניגוד לקמפיין ב-`reactivate.py` שהוא batch) — אבל **גם כאן
     חלה מדיניות חלון-24-שעות של מטא**: אם הליד לא כתב הודעה ב-24 השעות האחרונות, זו
     "business-initiated conversation" שעלולה לדרוש תבנית מאושרת; הקוד לא בודק את זה
     אוטומטית, זו אחריות הנציג השולח.
  ההיסטוריה בכרטיס הלקוח מסמנת כל הודעה עם `direction: "in"` (מהלקוח) או `"out"` (מהמערכת).
  שליחה ידנית דרך `log_manual_reply` (בניגוד ל-`update_lead_status`) **לא** משנה את
  `lead_status` — תגובה לליד "חם" לא אמורה להחזיר אותו ל"נוצר קשר".
  **שדה `simulated`:** אם Twilio חוסם שליחה מ-`/api/messages/send` בגלל מגבלת חשבון Trial
  (סטטוס 400/422 עם "trial" בהודעה - למשל נמען לא מאומת) - זו לא נחשבת שגיאת קוד. השרת
  תופס את זה (`_is_trial_restriction` ב-`server.py`), **רושם את ההודעה בכל זאת** ב-
  `customers.json` וב-`messages` (SQLite) עם `simulated: true`/`simulated=1`, ומחזיר `200`
  לדשבורד (לא `502`) - כדי שאפשר יהיה לבדוק את חלון השיחות בלי שכל בדיקה עם מספר לא-מאומת
  תיכשל. **הלקוח לא קיבל את ההודעה בפועל** במקרה הזה; ה-UI מציג את זה בבירור (בועה עם
  מסגרת מקווקוות כתומה + "⚠️ סימולציה - לא נשלח בפועל"). זה חל **רק** על `/api/messages/send`
  (השליחה הידנית מהדשבורד) - לא על `reactivate.py`/`--send`, ששם כשל שליחה עדיין מדווח
  ככשל אמיתי (`entry["error"]`), כי קמפיין batch לא אמור "להעלים" כשלים בלי שהמפעיל ידע.
- **⚠️ פרטי Twilio אמיתיים מוגדרים ב-`.env`** (`TWILIO_ACCOUNT_SID`/`TWILIO_AUTH_TOKEN`/
  `TWILIO_WHATSAPP_FROM`) — מאז 2026-08-25 אלה כבר לא ריקים. כל שימוש ב-`send_whatsapp_message`
  (מ-`reactivate.py --send` או מ-`/api/messages/send`) שולח **הודעת WhatsApp אמיתית** דרך
  Twilio. בזמן פיתוח/בדיקה - אל תשלחו למספרים אמיתיים בלי אישור מפורש; מספרים שלא הצטרפו
  ל-Sandbox (לא שלחו `join <קוד>`) יידחו אוטומטית ע"י Twilio כ"unverified" - זו רשת ביטחון
  חלקית לבדיקות, לא תחליף לזהירות.
- **`AGENT_PHONE_NUMBER`/`TWILIO_VOICE_FROM`/`PUBLIC_BASE_URL` (2026-08-27, ל-Click-to-Call
  ב-`voice_call.py`) - עדיין לא מוגדרים ב-`.env`.** בניגוד ל-`ANTHROPIC_API_KEY` (נבדק
  קשיח בזמן import, מפיל את השרת אם חסר), אלה נבדקים **רק בזמן קריאה** (בתוך
  `start_bridge_call`) - Voice הוא feature אופציונלי, והשרת ממשיך לעלות ולשרת WhatsApp
  גם בלעדיהם (`/api/calls/start` פשוט חוזר במצב `simulated`). `PUBLIC_BASE_URL` בלי `/`
  בסוף.

## חלוקת סוכנים

### 1. DevOps Agent
**אחריות:** הרצת הקוד, ניהול השרת (`server.py`), התקנת תלויות, הרצת בדיקות.
**קבצים:** `server.py`, `requirements.txt`, `venv/`
**סמכות:** להפעיל/לכבות את השרת, להתקין חבילות, להריץ בדיקות.
**אסור:** לגעת בנתוני לקוחות (`customers.json`) ישירות — רק דרך הפונקציות ב-`extract.py`.

### 2. QA & Bug Finder Agent
**אחריות:** ניטור לוגים של `server.py`, איתור כשלים בחילוץ AI (JSON לא תקין, שדות חסרים),
שיפור הפרומפט (`EXTRACTION_PROMPT` ב-`extract.py`) בהתאם לכשלים שנצפו בפועל.
**קבצים:** `extract.py` (פרומפטים), לוגים.
**סמכות:** להציע/לערוך פרומפטים.
**אסור:** לשנות לוגיקת עסקים (upsert, מבנה הכרטיס) בלי אישור אנושי.

### 3. Cold Lead Reactivation Agent
**אחריות:** ניהול פנייה ללידים קרים מ-`contacts.csv`, ניסוח הודעות פתיחה מותאמות אישית,
שליחתן בפועל בוואטסאפ דרך Twilio, וסיווג תגובות נכנסות ל-`hot`/`not_relevant`.
**קבצים:** `reactivate.py`, `contacts.csv`, `whatsapp_send.py` (משותף עם `server.py`)
**גם דרך הדשבורד:** `POST /api/reactivate` ב-`server.py` קורא ל-`reactivate.run_reactivation_campaign`
(אותה פונקציה, לא שכפול) - ומופעל מפאנל "🔥 החייאת לידים קרים" ב-`index.html`. ה-UI אוכף
שני שלבים: קודם `{"send": false}` (תצוגה מקדימה חובה - מציגה את כל ההודעות שייווצרו), ורק
אחרי `confirm()` מפורש בדפדפן נשלחת קריאת `{"send": true}` בפועל. **אין** דרך ב-UI לדלג
ישר לשליחה בלי לעבור קודם דרך התצוגה המקדימה.
**סמכות:** לעדכן `lead_status` דרך `update_lead_status`; לשלוח הודעת פתיחה ליד קר **רק**
כשמריצים את הסקריפט עם `--send`, או לוחצים "שלח בפועל" בדשבורד אחרי תצוגה מקדימה
(ברירת המחדל בשני המקומות היא dry-run - תצוגה מקדימה, אין שליחה).
**אסור:** להריץ `--send`/לקרוא ל-`/api/reactivate` עם `send:true` בלי אישור אנושי מפורש
לכל קמפיין ידני; לשלוח למספר production אמיתי (מחוץ ל-Sandbox) בלי הודעת-תבנית מאושרת
מול מטא - שליחת טקסט חופשי כפנייה יזומה שם תיחסם או תסכן את המספר.

**⚠️ עדכון 2026-08-25 - אוטומציה מתוזמנת קיימת עכשיו, אבל כבויה כברירת מחדל:**
`scheduler.py` הוא daemon thread שרץ בתוך `server.py` ומפעיל את `reactivate.run_reactivation_campaign`
**אוטומטית**, בלי קליק אנושי בכל מחזור - זה חריג מכוון לכלל "לא אוטומציה מתוזמנת" למעלה,
והוא בטוח **רק** כי ברירת המחדל שלו היא dry-run תמידי (סורק, מייצר הודעות, רושם ל-
`chat_history.txt` ולטבלת `scheduler_runs`, אבל לא שולח). שליחה אוטומטית אמיתית דורשת
`SCHEDULER_AUTO_SEND=true` מפורש ב-`.env` (כרגע **לא** מוגדר - ראו האזהרה המלאה בראש
`scheduler.py`). **אל תדליקו את זה** בלי לוודא קודם הודעות-תבנית מאושרות מול מטא לכל
ורטיקל, ובלי החלטה מודעת שסבב שליחה ללא אישור נקודתי מקובל עסקית כאן.
הסף לכל ורטיקל (`VERTICAL_COLD_THRESHOLDS_DAYS` ב-`scheduler.py`): `ecommerce`=3 ימים,
`real_estate`=14 ימים בלי קשר. `contacts.csv` מכיל עמודת `vertical` אופציונלית (ברירת
מחדל: `ecommerce`) - זו לוגיקה חדשה, לא הייתה קיימת קודם בנתוני הפרויקט.

### 4. Sales & Objection Agent
**אחריות:** ניהול שיחת מכירה, זיהוי וטיפול בהתנגדויות נפוצות ("יקר לי", "צריך לחשוב",
"לא עכשיו") בהתאם להיסטוריית השיחה בכרטיס הלקוח.
**קבצים:** טרם נוצר — מועמד: `sales_agent.py`
**סמכות:** להציע תסריטי מענה להתנגדויות.
**אסור:** לסגור עסקה, להתחייב על מחיר/הנחה, או לשלוח הודעה בלי אישור אנושי.

### 5. Scheduling & Follow-Up Agent
**אחריות:** ניהול תזכורות מעקב (follow-up) ללידים שלא ענו, קביעת פגישות ביומן.
**קבצים:** טרם נוצר — מועמד: `scheduling_agent.py`
**סמכות:** לקרוא/לכתוב ליומן (כשייבנה חיבור בפועל — Google Calendar API וכו').
**אסור:** למחוק או לשנות פגישות קיימות בלי אישור אנושי.

### 6. BI & Analytics Agent
**אחריות:** הפקת דוחות ביצועים — יחסי המרה לפי ערוץ, זמן תגובה ממוצע, זיהוי צווארי בקבוק
בתהליך המכירה.
**קבצים:** טרם נוצר — מועמד: `analytics.py`, קורא מ-`customers.json`
**סמכות:** קריאה בלבד (read-only).
**אסור:** לשנות נתוני לקוחות בכל צורה שהיא.
